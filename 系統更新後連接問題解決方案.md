# 系統更新後無法連接 iPhone/iPad 的解決方案

## 🔍 問題診斷

### 檢查結果：
1. ✅ pymobiledevice3 已安裝並正在運行
2. ⚠️ 端口 49151 未被佔用（隧道可能未正常啟動）
3. ⚠️ 未檢測到 USB 設備（可能未連接或未信任）

## 🔧 解決步驟

### 步驟 1: 確認設備連接
1. **檢查 USB 線**：
   - 使用原廠 USB 線
   - 嘗試不同的 USB 端口
   - 確認 USB 線可以傳輸數據（不只是充電）

2. **檢查 iPhone/iPad**：
   - 設備已解鎖
   - 設備已解鎖螢幕
   - 沒有進入恢復模式

### 步驟 2: 重新信任電腦（最重要！）

系統更新後，iPhone/iPad 可能會要求重新信任電腦：

1. **在 iPhone/iPad 上**：
   - 連接 USB 後，會彈出「信任此電腦？」提示
   - 點擊「信任」
   - 輸入設備密碼確認

2. **如果沒有彈出提示**：
   - 前往「設定」>「一般」>「傳輸或重置 iPhone」
   - 點擊「重置」>「重置位置與隱私」
   - 重新連接 USB，應該會再次彈出信任提示

### 步驟 3: 清除舊的連接

在 Mac 上執行：
```bash
# 1. 結束所有 pymobiledevice3 進程
sudo killall -9 pymobiledevice3

# 2. 清除端口佔用
sudo lsof -i tcp:49151 -t | xargs sudo kill -9

# 3. 等待 2 秒
sleep 2
```

### 步驟 4: 重新啟動 App

1. 關閉 App
2. 重新打開 App
3. 點擊「初始化連線」
4. 輸入管理員密碼（如果需要）

### 步驟 5: 檢查系統權限

macOS 26.2 可能需要額外的權限：

1. **系統設定** > **隱私權與安全性**：
   - 確認「完整取用磁碟」已授權
   - 確認「輔助使用」已授權（如果需要）

2. **終端機權限**：
   - 如果使用終端機執行命令，可能需要授權

## 🆘 如果仍然無法連接

### 方案 A: 更新 pymobiledevice3
```bash
pip3 install --upgrade pymobiledevice3
```

### 方案 B: 檢查系統日誌
```bash
# 查看系統日誌中的錯誤
log show --predicate 'process == "pymobiledevice3"' --last 5m
```

### 方案 C: 手動測試連接
```bash
# 測試設備是否被系統識別
system_profiler SPUSBDataType | grep -i "iphone\|ipad"

# 測試 pymobiledevice3 是否能列出設備
sudo python3 -m pymobiledevice3 list-devices
```

## 📝 常見問題

### Q: 為什麼系統更新後就無法連接？
A: macOS 系統更新可能會：
- 重置 USB 權限
- 要求重新信任設備
- 更新安全策略

### Q: 如何確認設備已連接？
A: 執行以下命令：
```bash
system_profiler SPUSBDataType | grep -i "serial number"
```
如果看到序列號，表示設備已連接。

### Q: 為什麼端口 49151 未被佔用？
A: 可能原因：
- 隧道未成功啟動
- 設備未信任電腦
- pymobiledevice3 版本不兼容新系統
