# 🔧 完整修復指南

## ✅ 服務器已啟動

服務器已在後台運行，端口：3000

**檢查服務器狀態**：
```bash
curl http://localhost:3000/api/auth/check -X POST -H "Content-Type: application/json" -d '{"deviceId":"test"}'
```

---

## 🔧 需要修復的問題

### 問題 1：App 沒有顯示剩餘次數
**原因**：沒有在啟動時檢查授權狀態

**修復**：在 `ContentView.swift` 的 `init()` 中添加：

```swift
override init() {
    super.init()
    // 自動載入預設軌跡
    DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) {
        self.loadDefaultTrack()
    }
    // 啟動時檢查授權狀態
    DispatchQueue.main.asyncAfter(deadline: .now() + 2.0) {
        self.authManager.checkAuth { [weak self] _ in
            self?.updateTrialStatusInWebView()
        }
    }
    // 監聽授權狀態變化
    authManager.$remainingTrial
        .receive(on: DispatchQueue.main)
        .sink { [weak self] _ in
            self?.updateTrialStatusInWebView()
        }
        .store(in: &cancellables)
    
    authManager.$authStatus
        .receive(on: DispatchQueue.main)
        .sink { [weak self] _ in
            self?.updateTrialStatusInWebView()
        }
        .store(in: &cancellables)
}
```

**還需要添加**：
```swift
private var cancellables = Set<AnyCancellable>()
```

### 問題 2：App 沒有檢查授權就執行操作
**原因**：`tp` 和 `startRoute` 沒有檢查授權

**修復**：在 `userContentController` 中修改：

```swift
case "tp":
    // 檢查授權後執行瞬移
    self.authManager.performActionWithAuth { [weak self] success, errorMessage in
        guard let self = self else { return }
        if success {
            // 停止所有移動模式
            self.stopTimerOnly()
            self.stopStuckCheck()
            self.isUsingRealTrack = false
            // 執行瞬移
            self.teleport(lat: dict["la"] as? Double ?? 0, lon: dict["lo"] as? Double ?? 0)
            // 更新剩餘次數顯示
            DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                self.updateTrialStatusInWebView()
            }
        } else {
            // 顯示錯誤訊息
            DispatchQueue.main.async {
                let escapedMessage = (errorMessage ?? "試用次數已用完，請購買授權").replacingOccurrences(of: "'", with: "\\'")
                self.webView?.evaluateJavaScript("alert('\(escapedMessage)');")
                // 如果次數用完，顯示激活 UI
                self.webView?.evaluateJavaScript("updateTrialStatus(0, false);")
            }
        }
    }
case "startRoute":
    // 檢查授權後執行散花
    self.authManager.performActionWithAuth { [weak self] success, errorMessage in
        guard let self = self else { return }
        if success {
            let pts = dict["pts"] as? [[Double]] ?? []
            self.startCruise(points: pts, kmh: 18.0)
            // 更新剩餘次數顯示
            DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                self.updateTrialStatusInWebView()
            }
        } else {
            // 顯示錯誤訊息
            DispatchQueue.main.async {
                let escapedMessage = (errorMessage ?? "試用次數已用完，請購買授權").replacingOccurrences(of: "'", with: "\\'")
                self.webView?.evaluateJavaScript("alert('\(escapedMessage)');")
                // 如果次數用完，顯示激活 UI
                self.webView?.evaluateJavaScript("updateTrialStatus(0, false);")
            }
        }
    }
```

### 問題 3：沒有跳出來激活碼輸入
**已修復**：`index.html` 中的 `updateTrialStatus()` 函數已更新，會在次數用完時自動顯示激活 UI

---

## 📋 測試激活碼（5 組）

```
1. PKM-9A0EF1DD632BBC1D
2. PKM-CF711BA7A0DBC80C
3. PKM-2CA9C96C3A8AB6E8
4. PKM-FE6C8F8CB7F37339
5. PKM-F7EC3C6305756F6C
```

**已自動添加到 `auth_server.js`**，重啟服務器後即可使用。

---

## 💻 跨平台支援

### macOS ✅
- **所有 Mac 都可以用**
- 已完成並可正常使用

### Windows ⚠️
- **目前還不能用**
- 需要開發 Windows 版本
- **建議使用 Electron**（可以重用現有 HTML/JS 代碼）

---

## 🚀 如何運行和測試

### 1. 確認服務器運行

```bash
# 檢查服務器
curl http://localhost:3000/api/auth/check -X POST -H "Content-Type: application/json" -d '{"deviceId":"test"}'

# 如果沒有運行，啟動服務器
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本/server"
npm start
```

### 2. 在 Xcode 中運行 App

1. 打開 Xcode 專案
2. 選擇 `Pikmin_Dev_Portable` scheme
3. 按 `Cmd + R` 運行

### 3. 測試流程

1. **啟動 App**
   - 應該會自動檢查授權狀態
   - 頂部應該顯示「🎁 剩餘免費次數: 3 次」

2. **使用功能**
   - 點擊地圖，選擇位置
   - 點擊「GO」執行瞬移
   - 每次使用會消耗 1 次試用
   - 剩餘次數會自動更新

3. **次數用完**
   - 使用 3 次後，剩餘次數變為 0
   - 自動顯示激活 UI
   - 顯示「0 次（請購買授權）」
   - 可以輸入激活碼

4. **測試激活碼**
   - 使用測試激活碼：`PKM-9A0EF1DD632BBC1D`
   - 激活後應該顯示「已購買，無限制使用」

---

## 📝 修改的檔案

1. ✅ `index.html` - 已添加頂部狀態欄和更新函數
2. ⚠️ `ContentView.swift` - 需要手動添加授權檢查（見上方代碼）

---

**最後更新**：2026-01-13
