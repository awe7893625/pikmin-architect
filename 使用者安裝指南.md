# KongGoo 使用者安裝指南

## 系統需求
- macOS 11.0 或更新版本
- USB 連接線
- iPhone/iPad (iOS 16 或更新版本)

## 首次使用步驟

### 1. 下載並安裝
1. 從網站下載 `KongGoo.dmg`（約 140MB）
2. 雙擊 DMG 檔案
3. 將 `KongGoo.app` 拖曳到「應用程式」資料夾
4. 從「應用程式」資料夾啟動 KongGoo

### 2. 首次啟動
如果系統顯示「無法打開」或「已損毀」：
1. 前往「系統設定」→「隱私權與安全性」
2. 找到「已阻擋的應用程式」
3. 點擊「仍要打開」

### 3. 準備 iPhone/iPad
1. **解鎖設備**（保持螢幕開啟）
2. **用 USB 線連接到 Mac**
3. 在 iPhone 上點擊**「信任此電腦」**
4. **(iOS 16+) 開啟 Developer Mode**：
   - 前往「設定」→「隱私權與安全性」→「開發者模式」
   - 打開後需要重新啟動設備

### 4. 安裝 pymobiledevice3（如果需要）

如果 App 顯示「缺少 pymobiledevice3 模組」，請在終端機執行：

**方法 1：使用 pip（推薦）**
```bash
pip3 install pymobiledevice3
```

**方法 2：使用 Homebrew**
```bash
brew install libimobiledevice
pip3 install pymobiledevice3
```

安裝完成後，重新啟動 App 並點擊「初始化連線」。

### 5. 初始化連線
1. 確認設備已連接並信任
2. 點擊 App 中的「初始化連線」按鈕
3. 輸入 macOS 密碼（需要 sudo 權限來啟動隧道）
4. 等待顯示「iPhone 已連線」

## 常見問題

### Q: 為什麼需要輸入密碼？
A: 建立隧道需要 `sudo` 權限來啟動 `pymobiledevice3 remote tunneld` 服務。這是 iOS 定位模擬的必要步驟。

### Q: 為什麼需要安裝 pymobiledevice3？
A: 某些 Mac（特別是較舊的 macOS 版本）無法執行 App 內建的 Python，需要使用系統的 Python 和 pymobiledevice3。

### Q: 隧道建立失敗怎麼辦？
A: 請檢查：
1. iPhone 已解鎖並保持螢幕開啟
2. 已點擊「信任此電腦」
3. (iOS 16+) 已開啟 Developer Mode
4. 已安裝 pymobiledevice3（`pip3 install pymobiledevice3`）
5. 沒有其他程式佔用 49151 端口

### Q: 端口 49151 被佔用怎麼辦？
A: 在終端機執行：
```bash
sudo lsof -i tcp:49151 -t | xargs sudo kill -9
```
然後重新點擊「初始化連線」。

### Q: 顯示「未偵測到 iOS 設備」怎麼辦？
A: 請確認：
1. USB 線已正確連接（嘗試換條線或換 USB 孔）
2. iPhone 已解鎖
3. 已點擊「信任此電腦」
4. 在終端機執行 `python3 -m pymobiledevice3 usbmux list` 確認設備可被偵測

## 技術支援

如果以上步驟都無法解決問題，請提供以下資訊：
1. macOS 版本（「關於這台 Mac」）
2. iOS 版本
3. 終端機執行以下指令的輸出：
   ```bash
   python3 --version
   python3 -m pymobiledevice3 --version
   python3 -m pymobiledevice3 usbmux list
   ```

---

**最後更新**: 2026-01-27
