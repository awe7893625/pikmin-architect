# 打包發布注意事項（macOS DMG）

## ✅ 已完成設定

### 1. 雙架構 Python 支援
- App 內嵌 `arm64` + `x86_64` 兩個版本的 Python
- 自動偵測 CPU 架構並使用對應版本
- 修正舊機「Bad CPU type」問題

### 2. Vercel 環境變數
- `MAC_DMG_URL`: 指向 GitHub Release 下載連結
- 已設定為 `production` / `preview` / `development` 三個環境

### 3. GitHub Release
- DMG 檔案上傳到 GitHub Releases
- 目前版本: `v20260122-164839`（最新）
- 下載連結: `https://github.com/awe7893625/pikmin-architect/releases/download/v20260122-164839/ios-location-simulator-mac.dmg`

## 📦 打包流程

### 執行打包腳本
```bash
cd PikminArchitect＿繼續開發版本
NOTARY_ISSUER_ID="b627bc7d-4dd7-4d57-bae2-e35730c37071" \
NOTARY_KEY_ID="NG5D9GR926" \
NOTARY_KEY_PATH="/Users/rain/Desktop/AI資料/AuthKey_NG5D9GR926.p8" \
./release_macos_direct_dmg.sh
```

### 打包步驟
1. **Archive** - Xcode 編譯並封存
2. **Export** - 產出已簽名的 .app
3. **內嵌 Python** - 執行 `scripts/bundle_python_deps.sh`，內嵌雙架構 Python + pymobiledevice3 + libimobiledevice
4. **修正依賴路徑** - 將 Homebrew 絕對路徑改為相對路徑（`@executable_path` / `@loader_path`）
5. **重新簽名** - 簽名所有 dylib、bin、Python so
6. **Notarize** - 提交 Apple 公證
7. **Staple** - 釘選公證票據
8. **產出 DMG** - 建立已簽名的 DMG

### 產出位置
- DMG: `website/public/downloads/ios-location-simulator-mac.dmg`
- 大小: 約 137MB（雙架構 Python）

## 🚀 發布流程

### 1. 上傳到 GitHub Release
```bash
cd PikminArchitect＿繼續開發版本
VERSION="v$(date +%Y%m%d-%H%M%S)"
gh release create "$VERSION" \
  website/public/downloads/ios-location-simulator-mac.dmg \
  --title "KongGoo macOS (雙架構 Python)" \
  --notes "修正舊機 Bad CPU type 問題，內嵌 arm64 + x86_64 Python"
```

### 2. 更新 Vercel 環境變數
```bash
VERCEL_TOKEN="4Lcg8iu6Zp70xSFCCqS2ijeY"
PROJECT_ID="prj_Ryy4nn9t3KR5sNEByq62JVO3TAlt"
DOWNLOAD_URL="https://github.com/awe7893625/pikmin-architect/releases/download/$VERSION/ios-location-simulator-mac.dmg"

# 取得現有環境變數 ID
ENV_ID=$(curl -s -X GET "https://api.vercel.com/v10/projects/$PROJECT_ID/env" \
  -H "Authorization: Bearer $VERCEL_TOKEN" | \
  jq -r '.[] | select(.key=="MAC_DMG_URL") | .id' | head -1)

# 更新環境變數
curl -X PATCH "https://api.vercel.com/v10/projects/$PROJECT_ID/env/$ENV_ID" \
  -H "Authorization: Bearer $VERCEL_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"value\":\"$DOWNLOAD_URL\",\"target\":[\"production\",\"preview\",\"development\"]}"
```

### 3. 觸發 Vercel 重新部署（讓環境變數生效）
- 到 Vercel Dashboard 手動觸發，或
- 推送一個空 commit 到 `main` 分支

## ⚠️ 注意事項

### GitHub Release 下載連結
- GitHub Release 的 CDN 可能需要 **5-10 分鐘** 才能同步
- 如果下載回傳 404，等待一段時間後再試

### Vercel 環境變數
- 環境變數更新後需要**重新部署**才會生效
- 可以在 Vercel Dashboard 手動觸發部署

### DMG 大小限制
- GitHub 單檔限制: **100MB**（超過會警告，但 137MB 仍可上傳）
- Git LFS 不適用於 Release 資產
- 建議未來考慮使用外部儲存（S3 / R2 / Vercel Blob）

### 舊機相容性
- ✅ Intel Mac (x86_64): 使用 `python_x86_64`
- ✅ Apple Silicon (arm64): 使用 `python_arm64`
- ✅ 自動偵測 CPU 架構

## 🔍 驗證步驟

### 1. 檢查 DMG 公證狀態
```bash
spctl -a -t open --context context:primary-signature -v website/public/downloads/ios-location-simulator-mac.dmg
xcrun stapler validate website/public/downloads/ios-location-simulator-mac.dmg
```

### 2. 檢查 Python 架構
```bash
hdiutil attach website/public/downloads/ios-location-simulator-mac.dmg -nobrowse
lipo -archs /Volumes/KongGoo/KongGoo.app/Contents/Resources/python_arm64/bin/python3
lipo -archs /Volumes/KongGoo/KongGoo.app/Contents/Resources/python_x86_64/bin/python3
hdiutil detach /Volumes/KongGoo
```

### 3. 測試下載端點
```bash
curl -I https://pikmin-architect.vercel.app/downloads/ios-location-simulator-mac.dmg
# 應該回傳 302 轉址到 GitHub Release
```

## 📝 Vercel 環境變數清單

- `VERCEL_TOKEN`: `4Lcg8iu6Zp70xSFCCqS2ijeY`
- `VERCEL_PROJECT_ID`: `prj_Ryy4nn9t3KR5sNEByq62JVO3TAlt`
- `VERCEL_TEAM_ID`: `team_aDZBTaEejPcweAPfcMYF63rs`
- `MAC_DMG_URL`: GitHub Release 下載連結（自動更新）
- `ENABLE_ADMIN_CONSOLE`: （可選）

