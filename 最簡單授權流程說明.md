# 最簡單授權流程 - 像模擬器一樣簡單！

## 🎯 核心概念

**用戶不需要開任何本地服務！所有服務都在雲端自動運行。**

---

## 📋 完整流程（用戶視角）

```
1. 用戶訪問購買網站（已部署在雲端，24/7 運行）
   ↓
2. 點擊「立即購買」→ 選擇付款方式
   ↓
3. 跳轉到金流平台付款
   ↓
4. 付款成功後，自動生成簡單的激活碼（6位數字）
   格式：PKM-123456
   ↓
5. 顯示成功頁面，激活碼大字顯示
   ↓
6. 用戶下載 App（已編譯，內含授權服務器 URL）
   ↓
7. 用戶在 App 中輸入激活碼：PKM-123456
   ↓
8. App 連接到雲端授權服務器驗證
   ↓
9. 激活成功！永久授權
```

**用戶完全不需要開任何本地服務！**

---

## 🔧 技術架構

### 1. 授權服務器（必須部署到雲端）

- **位置**：`/server/auth_server.js`
- **部署**：Vercel / Railway / Render（免費）
- **功能**：
  - 驗證激活碼
  - 綁定設備
  - 管理試用次數

### 2. 網站服務器（必須部署到雲端）

- **位置**：`/website/server.js`
- **部署**：Vercel / Netlify（免費）
- **功能**：
  - 處理付款
  - 生成激活碼（6位數字）
  - 同步到授權服務器

### 3. App（用戶下載）

- **位置**：編譯後的 .dmg 或 .app
- **功能**：
  - 自動連接到雲端授權服務器
  - 輸入激活碼驗證
  - 永久授權

---

## ✅ 已實現的功能

### 1. 簡單的激活碼格式

**舊格式**（太長）：
- `PKM-9A0EF1DD632BBC1D`（16位十六進制）

**新格式**（簡單）：
- `PKM-123456`（6位數字，容易輸入）

### 2. 付款後自動生成

付款成功後，系統自動：
1. 生成 6 位數字激活碼
2. 同步到授權服務器
3. 顯示在成功頁面

### 3. App 中輸入激活

用戶在 App 中：
1. 輸入激活碼：`PKM-123456`
2. 點擊「激活」
3. 自動驗證並綁定設備

---

## 🚀 部署步驟（您需要做的）

### 步驟 1：部署授權服務器到雲端

**Vercel（推薦，免費）**：

```bash
cd server
npm install -g vercel
vercel login
vercel --prod
```

部署成功後會得到 URL，例如：
- `https://pikmin-auth-server.vercel.app`

### 步驟 2：部署網站服務器到雲端

**Vercel（推薦，免費）**：

```bash
cd website
npm install -g vercel
vercel login
vercel --prod
```

部署成功後會得到 URL，例如：
- `https://pikmin-website.vercel.app`

### 步驟 3：設定環境變數

在 Vercel 控制台設定：

**授權服務器**：
- `ADMIN_KEY` = 您的管理員密鑰

**網站服務器**：
- `AUTH_SERVER_URL` = `https://pikmin-auth-server.vercel.app`
- `ADMIN_KEY` = 與授權服務器相同的密鑰
- 金流相關環境變數（ECPAY_MERCHANT_ID 等）

### 步驟 4：更新 App 代碼

在 `AuthManager.swift` 中，將生產環境的 URL 改為您的 Vercel URL：

```swift
#else
// 生產環境：改為您部署的授權服務器地址
return "https://pikmin-auth-server.vercel.app/api"
#endif
```

### 步驟 5：編譯 App

在 Xcode 中：
1. 選擇 Release 配置
2. Archive → Export
3. 打包為 .dmg

---

## 📱 用戶使用流程

### 1. 購買

1. 訪問購買網站：`https://pikmin-website.vercel.app`
2. 點擊「立即購買」
3. 選擇付款方式（信用卡/ATM/超商）
4. 完成付款

### 2. 獲取激活碼

付款成功後，頁面顯示：
```
✅ 付款成功！

您的激活碼：
PKM-123456

請在 App 中輸入此激活碼以激活完整版功能。
```

### 3. 下載 App

點擊下載按鈕，下載 App。

### 4. 激活

1. 打開 App
2. 在激活 UI 中輸入：`PKM-123456`
3. 點擊「激活」
4. 完成！永久授權

---

## 🎯 優勢

### ✅ 簡單
- 激活碼只有 6 位數字，容易輸入
- 不需要 Email
- 不需要複雜的流程

### ✅ 自動化
- 付款後自動生成激活碼
- 自動同步到授權服務器
- 用戶只需要輸入一次

### ✅ 無需本地服務
- 所有服務都在雲端
- 用戶不需要開任何東西
- 24/7 自動運行

### ✅ 安全
- 激活碼綁定設備
- 每個激活碼只能使用一次
- 防止重複使用

---

## 🔒 安全機制

1. **設備綁定**：激活碼綁定到設備 ID（Mac 硬體 UUID）
2. **一次性使用**：每個激活碼只能激活一個設備
3. **雲端驗證**：所有驗證都在雲端進行，無法破解

---

## 📝 總結

1. **用戶購買** → 訪問雲端網站，付款
2. **獲取激活碼** → 付款成功後顯示 6 位數字激活碼
3. **下載 App** → 從網站下載
4. **輸入激活碼** → 在 App 中輸入 `PKM-123456`
5. **永久授權** → 完成！

**用戶完全不需要開任何本地服務！所有服務都在雲端自動運行。**

---

## 🚨 重要提醒

1. **授權服務器必須部署到雲端**（Vercel/Railway/Render）
2. **網站服務器必須部署到雲端**（Vercel/Netlify）
3. **App 必須連接到雲端授權服務器**（修改 AuthManager.swift）
4. **用戶不需要開任何本地服務**

完成部署後，用戶就可以像使用模擬器一樣簡單地購買和激活！
