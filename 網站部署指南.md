# Pikmin Architect 網站部署指南

## 快速開始

### 1. 安裝依賴
```bash
cd website
npm install
```

### 2. 設定環境變數
創建 `.env` 檔案並填入您的金流 API 金鑰（參考 `金流串接說明.md`）

### 3. 啟動服務器
```bash
npm start
```

網站將運行在 http://localhost:3001

## 部署到 Vercel（免費）

### 步驟 1：安裝 Vercel CLI
```bash
npm i -g vercel
```

### 步驟 2：登入 Vercel
```bash
vercel login
```

### 步驟 3：部署
```bash
cd website
vercel
```

### 步驟 4：設定環境變數
在 Vercel 控制台設定環境變數：
- `ECPAY_MERCHANT_ID`
- `ECPAY_HASH_KEY`
- `ECPAY_HASH_IV`
- 等等...

### 步驟 5：設定自訂網域（可選）
在 Vercel 控制台設定您的網域

## 部署到 Netlify（免費）

### 步驟 1：安裝 Netlify CLI
```bash
npm i -g netlify-cli
```

### 步驟 2：登入
```bash
netlify login
```

### 步驟 3：部署
```bash
cd website
netlify deploy --prod
```

## 跨平台支援說明

### macOS 版本
- 當前 App 是 macOS 原生應用（Swift/SwiftUI）
- 可以直接打包為 .dmg 檔案
- 支援 macOS 10.15 以上

### Windows 版本
**需要開發 Windows 版本**，有幾個選項：

1. **使用 Electron**（推薦）：
   - 將現有的 HTML/JavaScript 介面包裝成 Electron App
   - 可以重用大部分代碼
   - 跨平台支援

2. **使用 .NET MAUI**：
   - 需要重寫部分代碼
   - 原生性能更好

3. **使用 Flutter Desktop**：
   - 需要重寫大部分代碼
   - 跨平台支援好

### 建議方案
由於您的 App 已經有 HTML/JavaScript 介面（index.html），最簡單的方式是：

1. **使用 Electron 包裝現有代碼**
2. 將 Swift 的 GPS 模擬功能改為 Node.js 實現
3. 這樣可以同時支援 Mac 和 Windows

## 下載連結設定

在 `server.js` 中，下載連結指向：
- macOS: `/download/PikminArchitect.dmg`
- Windows: `/download/PikminArchitect.exe`

您需要：
1. 將編譯好的 App 上傳到服務器
2. 或使用雲端儲存（如 AWS S3、Google Cloud Storage）
3. 更新下載連結

## 功能檢查清單

- [x] 精美網站設計
- [x] 金流整合（綠界/藍新）
- [x] 授權碼生成和驗證
- [x] 下載連結
- [ ] Email 發送授權碼（需要整合 Email 服務）
- [ ] 數據庫儲存（目前使用內存，需要改為數據庫）
- [ ] Windows 版本開發
- [ ] 實際部署和測試

## 費用總結

### 免費方案
- **網站託管**：Vercel/Netlify 免費
- **域名**：可選（約 NT$ 300-500/年）
- **金流手續費**：每筆交易 2.5-2.75%

### 付費方案
- **網站託管**：NT$ 300-1000/月
- **域名**：NT$ 300-500/年
- **金流手續費**：每筆交易 2.5-2.75%

**建議**：先用免費方案測試，確認有穩定收入後再考慮付費方案。
