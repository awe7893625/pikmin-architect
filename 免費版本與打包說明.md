# Pikmin Architect 免費版本與打包說明

## 📦 當前版本狀態

### ✅ 可以打包供人下載使用

**當前版本包含：**
- ✅ macOS 原生應用（Swift/SwiftUI）
- ✅ 試用版功能（3 次免費試用）
- ✅ 授權系統（可升級到完整版）
- ✅ 瞬移功能
- ✅ 散花功能

### 📋 使用條件

#### 基本要求
1. **macOS 系統**：macOS 10.15（Catalina）以上
2. **Python 環境**：需要安裝 `pymobiledevice3`
   ```bash
   pip3 install pymobiledevice3
   ```
3. **iOS 設備**：需要連接 iPhone/iPad（透過 USB）
4. **開發者模式**：iOS 設備需要開啟「開發者模式」
5. **權限**：首次使用需要輸入系統管理員密碼（用於建立 USB 隧道）

#### 試用版限制
- **免費試用**：每個設備 3 次免費試用
- **功能限制**：試用版功能與完整版相同，但有限制次數
- **升級方式**：試用次數用完後，需要購買授權碼升級到完整版

#### 完整版功能
- ✅ 無限制使用所有功能
- ✅ 永久授權（綁定設備）
- ✅ 優先技術支援

---

## 🆓 免費版本配置

### 版本類型

#### 1. 試用版（Trial Version）⭐ 當前版本
- **特點**：每個設備 3 次免費試用
- **功能**：與完整版相同，但有限制次數
- **升級**：試用次數用完後可購買授權碼

#### 2. 功能限制版（Feature-Limited Free Version）
- **特點**：永久免費，但功能有限制
- **可能限制**：
  - 每次使用只能移動 5 分鐘
  - 每天只能使用 3 次
  - 無法使用某些進階功能

### 當前版本是「試用版」

**試用版配置：**
- ✅ 自動註冊設備（首次啟動時）
- ✅ 自動獲得 3 次免費試用
- ✅ 每次使用核心功能（瞬移、散花）消耗 1 次試用
- ✅ 試用次數顯示在「授權」標籤頁
- ✅ 試用次數用完後提示購買授權

---

## 📦 打包流程

### 方法 1：使用 Xcode Archive（推薦）

#### 步驟 1：準備專案
1. 打開 Xcode
2. 打開專案：`Pikmin_Dev_Portable.xcodeproj`
3. 選擇 Scheme：`Pikmin_Dev_Portable`
4. 選擇目標：`My Mac` 或 `Any Mac`

#### 步驟 2：Archive
1. 在 Xcode 選單：`Product` → `Archive`
2. 等待編譯完成（約 1-2 分鐘）
3. 在 Organizer 視窗中選擇 Archive

#### 步驟 3：導出 App
1. 點擊 `Distribute App`
2. 選擇 `Copy App`（直接複製 .app 檔案）
3. 選擇導出位置
4. 完成後會得到 `Pikmin_Dev_Portable.app`

#### 步驟 4：創建 DMG（可選）
```bash
# 創建 DMG 檔案
hdiutil create -volname "Pikmin Architect" -srcfolder "Pikmin_Dev_Portable.app" -ov -format UDZO "PikminArchitect.dmg"
```

### 方法 2：使用命令行打包

```bash
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本"

# 編譯 Release 版本
xcodebuild -project Pikmin_Dev_Portable.xcodeproj \
  -scheme Pikmin_Dev_Portable \
  -configuration Release \
  -derivedDataPath ./build

# 找到編譯好的 App
# 位置：./build/Build/Products/Release/Pikmin_Dev_Portable.app
```

### 方法 3：使用腳本自動打包

創建 `build.sh`：

```bash
#!/bin/bash

PROJECT_NAME="Pikmin_Dev_Portable"
SCHEME="Pikmin_Dev_Portable"
BUILD_DIR="./build"
OUTPUT_DIR="./dist"

# 清理舊的建置
rm -rf "$BUILD_DIR"
rm -rf "$OUTPUT_DIR"
mkdir -p "$OUTPUT_DIR"

# 編譯
xcodebuild -project "${PROJECT_NAME}.xcodeproj" \
  -scheme "$SCHEME" \
  -configuration Release \
  -derivedDataPath "$BUILD_DIR" \
  clean build

# 複製 App
APP_PATH="$BUILD_DIR/Build/Products/Release/${PROJECT_NAME}.app"
if [ -d "$APP_PATH" ]; then
  cp -R "$APP_PATH" "$OUTPUT_DIR/"
  echo "✅ App 已複製到 $OUTPUT_DIR"
  
  # 創建 DMG
  DMG_NAME="PikminArchitect-v1.0.dmg"
  hdiutil create -volname "Pikmin Architect" \
    -srcfolder "$OUTPUT_DIR/${PROJECT_NAME}.app" \
    -ov -format UDZO "$OUTPUT_DIR/$DMG_NAME"
  
  echo "✅ DMG 已創建：$OUTPUT_DIR/$DMG_NAME"
else
  echo "❌ 找不到編譯好的 App"
  exit 1
fi
```

使用：
```bash
chmod +x build.sh
./build.sh
```

---

## 🔧 打包前檢查清單

### 必須檢查
- [ ] 確認 `AuthManager.swift` 中的 `apiBaseURL` 已設定為正確的服務器地址
- [ ] 確認所有功能都已加入授權檢查
- [ ] 測試試用功能是否正常
- [ ] 測試授權碼激活是否正常
- [ ] 確認 App 圖標已設定
- [ ] 確認版本號已更新

### 建議檢查
- [ ] 測試在不同 macOS 版本上運行
- [ ] 確認所有依賴都已包含
- [ ] 準備使用說明文件
- [ ] 準備授權碼購買連結

---

## 📝 打包後的檔案結構

```
PikminArchitect-v1.0.dmg
└── Pikmin_Dev_Portable.app
    ├── Contents/
    │   ├── Info.plist
    │   ├── MacOS/
    │   │   └── Pikmin_Dev_Portable (執行檔)
    │   ├── Resources/
    │   │   ├── index.html
    │   │   └── Assets.xcassets/
    │   └── Frameworks/
    └── ...
```

---

## 🚀 分發方式

### 方式 1：直接下載
1. 將 DMG 上傳到雲端儲存（Google Drive、Dropbox、iCloud）
2. 在網站上提供下載連結
3. 用戶下載後雙擊 DMG 安裝

### 方式 2：網站整合
1. 將 DMG 放在網站的下載目錄
2. 在購買頁面提供下載連結
3. 付款成功後自動顯示下載連結

### 方式 3：GitHub Releases
1. 在 GitHub 創建 Release
2. 上傳 DMG 檔案
3. 提供下載連結

---

## ⚠️ 注意事項

### 1. 代碼簽名（可選但建議）
- **未簽名**：用戶下載後可能看到「無法打開，因為來自身份不明的開發者」
- **解決方法**：用戶需要在「系統偏好設定」→「安全性與隱私」中允許
- **簽名**：需要 Apple Developer 帳號（年費 NT$ 3,390）

### 2. 公證（Notarization）（可選）
- 需要 Apple Developer 帳號
- 可以避免 macOS 的安全警告
- 適合正式發布

### 3. 依賴問題
- **pymobiledevice3**：需要用戶自行安裝
- **Python 3**：需要用戶已安裝
- 建議在 README 中說明安裝步驟

### 4. 授權服務器
- 試用版需要連接到授權服務器
- 確保服務器已部署並可訪問
- 建議使用 HTTPS

---

## 📋 用戶使用流程

### 首次使用
1. **下載 App**：從網站下載 DMG
2. **安裝**：雙擊 DMG，將 App 拖到 Applications
3. **首次啟動**：
   - 可能看到安全警告（未簽名）
   - 在「系統偏好設定」→「安全性與隱私」中允許
4. **自動註冊**：App 會自動註冊設備並獲得 3 次免費試用
5. **開始使用**：可以使用瞬移和散花功能

### 試用期間
- 每次使用核心功能會消耗 1 次試用
- 剩餘次數顯示在「授權」標籤頁
- 可以隨時購買授權碼升級

### 升級到完整版
1. 在網站購買授權碼
2. 在 App 的「授權」標籤頁輸入授權碼
3. 點擊「激活授權碼」
4. 激活成功後可無限制使用

---

## 🎯 建議配置

### 免費版本配置（當前）

**試用版設定：**
```swift
// AuthManager.swift
private let apiBaseURL = "https://your-server.com/api" // 改為您的服務器地址
```

**功能限制：**
- ✅ 3 次免費試用
- ✅ 每次使用消耗 1 次試用
- ✅ 試用次數用完後提示購買

### 完整版配置

**授權檢查：**
- ✅ 已付費用戶無限制使用
- ✅ 授權碼綁定設備
- ✅ 防止授權碼被重複使用

---

## 📞 技術支援

### 用戶常見問題

1. **「無法打開 App」**
   - 解決：系統偏好設定 → 安全性與隱私 → 允許

2. **「找不到 pymobiledevice3」**
   - 解決：`pip3 install pymobiledevice3`

3. **「無法連接設備」**
   - 解決：確保 iOS 設備已開啟開發者模式

4. **「試用次數已用完」**
   - 解決：購買授權碼升級到完整版

---

## ✅ 打包檢查清單

### 打包前
- [ ] 更新版本號
- [ ] 設定正確的 API 服務器地址
- [ ] 測試所有功能
- [ ] 測試授權系統
- [ ] 準備使用說明

### 打包後
- [ ] 測試 DMG 安裝流程
- [ ] 測試首次啟動
- [ ] 測試試用功能
- [ ] 測試授權激活
- [ ] 上傳到下載位置

---

## 🎉 完成！

現在您可以：
1. ✅ 打包 App 供人下載
2. ✅ 提供免費試用（3 次）
3. ✅ 用戶可以購買授權碼升級
4. ✅ 開始銷售！

**下一步：**
1. 打包 App（使用上面的方法）
2. 部署授權服務器
3. 上傳到網站供下載
4. 開始宣傳和銷售！
