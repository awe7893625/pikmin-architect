# 🚀 部署和更新說明

## 📋 本次修改內容

### 1. ✅ 網站更新 (`website/index.html`)
- **免費試用說明**：在首頁強調「免費試用 3 次，無需信用卡」
- **App 介面展示**：新增精美的 App 介面預覽區塊
- **操作說明**：新增詳細的操作步驟說明
- **種花功能限制**：明確說明散花功能只能維持 5 分鐘
- **儲存喜愛地點**：特別強調可以儲存喜愛地點，之後可以再飛過來
- **付款流程說明**：詳細說明使用次數滿後如何進行付款

### 2. ✅ App 更新 (`PikminArchitect/index.html` 和 `ContentView.swift`)
- **剩餘次數顯示**：在授權頁面顯示剩餘免費試用次數
- **即時更新**：當授權狀態改變時，自動更新剩餘次數顯示
- **授權狀態處理**：新增 `checkAuth` 和 `activateLicense` 消息處理

### 3. ✅ API 金鑰文件更新
- 在 `API_KEYS_COMPLETE.md` 中加入 Vercel token

---

## 🚀 如何部署網站更新

### 方法 1：使用 Git 推送（推薦）

```bash
# 1. 進入專案目錄
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本"

# 2. 檢查修改狀態
git status

# 3. 添加修改的檔案
git add website/index.html
git add "PikminArchitect/index.html"
git add "PikminArchitect/ContentView.swift"

# 4. 提交修改
git commit -m "更新網站：加入免費試用說明、App 介面展示、操作說明、付款流程"

# 5. 推送到 GitHub
git push origin main
```

**Vercel 會自動檢測到推送並重新部署網站**（約 1-2 分鐘）

### 方法 2：使用 Vercel CLI 手動部署

```bash
# 1. 進入網站目錄
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本/website"

# 2. 使用 Vercel token 部署
export VERCEL_TOKEN="RAJYqeo7mSsADDhlnzoMeEen"
vercel --prod --yes --token="$VERCEL_TOKEN"
```

---

## 📱 如何更新 App

### 1. 重新編譯 App

```bash
# 進入專案目錄
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本"

# 使用 Xcode 打開專案
open PikminArchitect.xcodeproj
```

### 2. 在 Xcode 中編譯

1. **選擇目標**：選擇 `PikminArchitect` scheme
2. **選擇設備**：選擇 `My Mac` 或連接的 iOS 設備
3. **編譯**：按 `Cmd + B` 編譯
4. **運行**：按 `Cmd + R` 運行測試

### 3. 打包分發版本

```bash
# 使用提供的打包腳本
./build.sh
```

這會：
- 創建 Archive
- 導出 `.app` 檔案
- 創建 DMG 安裝包

### 4. 更新下載連結

打包完成後，將新的 DMG 檔案上傳到：
- GitHub Releases
- 雲端儲存（Google Drive、Dropbox 等）
- 或您的下載伺服器

然後更新 `website/server.js` 中的下載連結。

---

## 🔄 更新流程檢查清單

### 網站更新
- [ ] 修改 `website/index.html`
- [ ] 測試本地預覽（`cd website && npm start`）
- [ ] 提交到 Git
- [ ] 推送到 GitHub
- [ ] 等待 Vercel 自動部署（約 1-2 分鐘）
- [ ] 訪問 https://konggoo.tw 確認更新（建議設定 `PRIMARY_HOST=konggoo.tw` 讓舊域名 301 導向）

### App 更新
- [ ] 修改 `PikminArchitect/index.html` 或 `ContentView.swift`
- [ ] 在 Xcode 中編譯測試
- [ ] 確認功能正常（剩餘次數顯示、授權檢查等）
- [ ] 使用 `build.sh` 打包
- [ ] 上傳新的 DMG 檔案
- [ ] 更新下載連結（如果需要）

---

## 📝 測試要點

### 網站測試
1. ✅ 確認「免費試用 3 次」說明清晰可見
2. ✅ 確認 App 介面展示正常顯示
3. ✅ 確認操作說明完整
4. ✅ 確認種花功能 5 分鐘限制說明存在
5. ✅ 確認付款流程說明清楚
6. ✅ 確認「儲存喜愛地點」功能被強調

### App 測試
1. ✅ 確認授權頁面顯示剩餘次數
2. ✅ 確認使用功能後，剩餘次數正確減少
3. ✅ 確認檢查授權按鈕正常工作
4. ✅ 確認激活授權碼功能正常
5. ✅ 確認次數用完後顯示正確提示

---

## 🐛 常見問題

### Q: Vercel 沒有自動部署？
**A:** 檢查：
1. GitHub 推送是否成功
2. Vercel 專案是否正確連結到 GitHub 倉庫
3. 前往 Vercel 控制台查看部署日誌

### Q: App 編譯錯誤？
**A:** 檢查：
1. 是否正確導入 `Combine` framework
2. `cancellables` 變數是否正確聲明
3. `updateTrialStatusInWebView` 函數是否正確實現

### Q: 剩餘次數不顯示？
**A:** 檢查：
1. `AuthManager` 是否正確初始化
2. `checkAuth` 是否成功調用
3. JavaScript 函數 `updateTrialStatus` 是否正確定義

---

## 📞 需要幫助？

如有任何問題，請檢查：
1. **Git 狀態**：`git status`
2. **編譯錯誤**：Xcode 的 Issue Navigator
3. **部署日誌**：Vercel 控制台的 Deployments 頁面
4. **瀏覽器控制台**：檢查 JavaScript 錯誤

---

**最後更新**：2026-01-13
