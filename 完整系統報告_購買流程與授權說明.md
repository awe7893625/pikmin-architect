# 摃菇 iOS 模擬器 (KongGoo) - 完整系統報告

**更新日期：** 2025年1月14日  
**版本：** 最新版本  
**狀態：** ✅ 所有功能已更新並測試完成

---

## 📋 目錄

1. [系統概述](#系統概述)
2. [購買流程完整說明](#購買流程完整說明)
3. [授權系統詳細說明](#授權系統詳細說明)
4. [App 內設置功能](#app-內設置功能)
5. [語言切換功能](#語言切換功能)
6. [技術架構](#技術架構)
7. [部署狀態](#部署狀態)
8. [已知問題與解決方案](#已知問題與解決方案)

---

## 🎯 系統概述

### 應用程式資訊
- **中文名稱：** 摃菇 iOS 模擬器
- **英文名稱：** KongGoo
- **網站：** KongGo.Tw
- **Bundle ID：** `com.pikmin.pro.KongGoo`
- **平台：** macOS（Windows 版本開發中）

### 核心功能
1. **瞬移模式：** 立即將 iOS 設備位置跳轉到指定座標
2. **散花模式：** 沿著預設路徑自動移動設備位置
3. **最愛地點：** 儲存常用位置並快速訪問
4. **地圖搜尋：** 透過 OpenStreetMap API 搜尋地點

---

## 💳 購買流程完整說明

### 流程圖

```
用戶打開 App
    ↓
使用 3 次免費試用
    ↓
試用次數用完（顯示「0 次（請購買授權）」）
    ↓
點擊「💳 購買授權」按鈕
    ↓
選擇付款方案（年費 NT$690 或永久 NT$1,750）
    ↓
跳轉到付款頁面（Polar 或模擬付款）
    ↓
完成付款
    ↓
系統自動生成授權碼（格式：PKM-XXXXXXXX）
    ↓
顯示授權碼在付款成功頁面
    ↓
用戶複製授權碼
    ↓
在 App 中輸入授權碼並激活
    ↓
設備永久激活，無限制使用
```

### 詳細步驟

#### 步驟 1：觸發購買
- **位置：** App 內右下角 🔑 設定按鈕 → 「💳 購買授權」按鈕
- **或：** 試用次數用完時，授權 UI 中會顯示「💳 購買授權」按鈕

#### 步驟 2：選擇付款方案
- **年費方案：** NT$ 690（1 年授權，推薦）
- **永久方案：** NT$ 1,750（永久授權）

#### 步驟 3：付款處理
- **如果已設定 Polar 金流：**
  - 跳轉到 Polar 付款頁面
  - 完成付款後，Polar 會發送 webhook 通知
  - 系統自動生成授權碼
- **如果未設定 Polar（測試模式）：**
  - 使用模擬付款流程
  - 點擊「模擬付款成功」按鈕
  - 系統自動生成授權碼

#### 步驟 4：獲取授權碼
- **付款成功後：** 自動跳轉到 `/payment/success` 頁面
- **顯示內容：**
  - ✅ 付款成功提示
  - 📋 授權碼（格式：`PKM-XXXXXXXX`）
  - 📋 複製按鈕
  - 📋 使用說明

#### 步驟 5：激活授權碼
1. 打開 App
2. 點擊右下角 🔑 設定按鈕
3. 在「激活授權碼」欄位中輸入授權碼
4. 點擊「✅ 激活授權碼」按鈕
5. 等待激活成功提示

### 付款系統技術細節

#### API 端點
- **創建訂單：** `POST /api/payment/create`
  - 參數：`{ planType: 'annual' | 'lifetime' }`
  - 返回：`{ success: true, orderId: string, paymentUrl: string }`

#### Polar 金流整合
- **環境變數：**
  - `POLAR_ACCESS_TOKEN`：Polar API 訪問令牌
  - `POLAR_PRODUCT_PRICE_ID_ANNUAL`：年費方案價格 ID
  - `POLAR_PRODUCT_PRICE_ID_LIFETIME`：永久方案價格 ID
- **Webhook：** `POST /api/payment/polar-webhook`
  - 接收 Polar 付款成功通知
  - 自動生成授權碼
  - 更新訂單狀態

#### 授權碼生成規則
- **格式：** `PKM-` + 16 位十六進制大寫字母
- **範例：** `PKM-9A0EF1DD632BBC1D`
- **唯一性：** 每個授權碼只能使用一次（綁定到單一設備）

---

## 🔐 授權系統詳細說明

### 授權機制

#### 1. 設備識別
- **識別方式：** Mac Hardware UUID
- **獲取方法：** `system_profiler SPHardwareDataType | grep 'Hardware UUID'`
- **特點：** 每台 Mac 有唯一的 UUID，無法偽造

#### 2. 試用系統
- **初始試用次數：** 3 次
- **消耗時機：** 每次使用「瞬移」或「散花」功能時
- **顯示位置：** 右上角授權狀態欄
- **狀態顯示：**
  - `3 次`、`2 次`、`1 次`：還有試用次數
  - `0 次（請購買授權）`：試用次數用完，顯示購買 UI

#### 3. 授權激活
- **激活方式：** 輸入授權碼並點擊「激活」按鈕
- **API 端點：** `POST /api/license/activate`
- **參數：** `{ deviceId: string, licenseKey: string }`
- **驗證邏輯：**
  1. 檢查授權碼是否存在
  2. 檢查授權碼是否有效
  3. 檢查授權碼是否已被其他設備使用（每個授權碼只能使用一次）
  4. 綁定授權碼到設備 UUID
  5. 標記設備為已激活

#### 4. 授權狀態檢查
- **API 端點：** `POST /api/auth/check`
- **觸發時機：**
  - App 啟動時自動檢查
  - 手動點擊「🔍 檢查授權」按鈕
- **返回數據：**
  ```json
  {
    "trialCount": 3,
    "licenseKey": "PKM-XXXXXXXX",
    "isActivated": true
  }
  ```

### 授權狀態判斷邏輯

```javascript
// 如果設備有授權碼，就表示已購買，應該無限制使用
let isPaid = isActivated || (licenseKey != null && !licenseKey.isEmpty)

if (isPaid) {
    // 已激活/已購買，顯示為已付費，無限制使用
    // 隱藏所有試用次數顯示和授權 UI
} else {
    // 未激活，顯示試用次數
    // 如果試用次數為 0，顯示購買 UI
}
```

### 授權 UI 顯示規則

#### 已激活用戶
- ✅ 隱藏「剩餘免費次數」狀態欄
- ✅ 隱藏授權 UI（購買按鈕、激活輸入框）
- ✅ 顯示「已購買，無限制使用」（僅在設定中）

#### 未激活用戶（有試用次數）
- ✅ 顯示「剩餘免費次數：X 次」
- ❌ 隱藏授權 UI（不顯示購買按鈕）

#### 未激活用戶（試用次數用完）
- ✅ 顯示「剩餘免費次數：0 次（請購買授權）」
- ✅ 顯示授權 UI（購買按鈕、激活輸入框）

### 授權服務器狀態

#### 當前部署
- **生產環境：** `https://konggoo.tw`
- **本地開發：** `http://localhost:3001`
- **狀態：** ✅ 已部署並運行正常

#### 數據存儲
- **當前實現：** 內存存儲（`Map` 結構）
- **生產環境建議：** 使用數據庫（MongoDB、PostgreSQL 等）
- **數據結構：**
  - `devices Map`：設備 UUID → 設備信息
  - `licenses Map`：授權碼 → 授權信息
  - `orders Map`：訂單 ID → 訂單信息

---

## ⚙️ App 內設置功能

### 設定模態框位置
- **觸發方式：** 點擊右下角 🔑 按鈕
- **內容：**
  1. **語言選擇下拉選單**
     - 繁體中文
     - English
     - 日本語
     - 한국어
  2. **激活授權碼輸入框**
     - 輸入授權碼
     - 點擊「✅ 激活授權碼」按鈕
  3. **💳 購買授權按鈕**
     - 打開付款選擇模態框
     - 選擇年費或永久方案
  4. **💾 備份最愛地點到檔案按鈕**
     - 將當前最愛地點備份到專案目錄
     - 檔案位置：`PikminArchitect/favorite_locations_backup.json`

### 授權 UI（主界面）
- **位置：** 右上角授權狀態區域
- **內容：**
  - 「🎁 剩餘免費次數：X 次」狀態顯示
  - 「🔍 檢查授權」按鈕
  - 「💳 購買授權」按鈕（僅在試用次數用完時顯示）
  - 「輸入授權碼」輸入框
  - 「✅ 激活授權碼」按鈕

### 付款選擇模態框
- **觸發方式：** 點擊「💳 購買授權」按鈕
- **內容：**
  - **年費方案卡片**
    - 價格：NT$ 690
    - 標籤：推薦
    - 說明：1 年授權
  - **永久方案卡片**
    - 價格：NT$ 1,750
    - 說明：永久授權
  - **前往付款按鈕**
    - 點擊後跳轉到付款頁面

---

## 🌐 語言切換功能

### 支援語言
1. **繁體中文 (zh-TW)** - 預設語言
2. **English (en)**
3. **日本語 (ja)**
4. **한국어 (ko)**

### 語言切換方式
1. **位置：** 右下角 🔑 設定按鈕 → 語言下拉選單
2. **操作：** 選擇語言後，介面立即切換
3. **儲存：** 語言選擇會儲存在 `localStorage` 中，下次啟動時自動載入

### 語言檔案位置
- **App 內：** `PikminArchitect/locales/{lang}.json`
- **網站：** `website/locales/{lang}.json`

### 翻譯覆蓋範圍

#### ✅ 已翻譯的 UI 元素
- 初始化連線提示
- 搜尋欄位提示
- 模式標籤（瞬移、散花、最愛）
- 按鈕文字（GO、STOP、加入目前位置）
- 授權相關文字（剩餘免費次數、檢查授權、激活授權碼）
- 設定模態框文字（語言、激活授權碼、購買授權、備份最愛地點）
- 付款相關文字（年費方案、永久方案、前往付款）

#### 翻譯實現機制
1. **HTML 標記：** 使用 `data-i18n` 屬性標記需要翻譯的元素
   ```html
   <div data-i18n="teleport">瞬移</div>
   ```

2. **語言載入：** `loadLanguage(lang)` 函數
   - 從 `locales/{lang}.json` 載入翻譯檔案
   - 支援多種路徑嘗試（確保在 WKWebView 中正常載入）

3. **翻譯應用：** `applyTranslations()` 函數
   - 遍歷所有帶有 `data-i18n` 屬性的元素
   - 根據當前語言更新文字內容

4. **語言切換：** `setLanguageFromSelect(lang)` 函數
   - 更新 `currentLang` 變數
   - 載入新語言檔案
   - 應用翻譯
   - 儲存到 `localStorage`

### 語言切換測試確認

#### ✅ 已實現的功能
- [x] 語言下拉選單可以選擇語言
- [x] 選擇語言後，介面文字立即更新
- [x] 語言選擇會儲存，下次啟動時自動載入
- [x] 所有主要 UI 元素都有對應的翻譯
- [x] 支援繁體中文、English、日本語、한국어

#### 語言切換實現細節
1. **語言載入機制：**
   - `loadLanguage(lang)` 函數從 `locales/{lang}.json` 載入翻譯檔案
   - 支援多種路徑嘗試，確保在 WKWebView 中正常載入
   - 載入成功後自動應用翻譯

2. **翻譯應用機制：**
   - `applyTranslations()` 函數遍歷所有帶有 `data-i18n` 屬性的元素
   - 更新元素文字內容和 placeholder 屬性
   - 強制重新渲染確保更新可見

3. **已添加 data-i18n 屬性的元素：**
   - 初始化連線提示
   - 搜尋欄位 placeholder
   - 模式標籤（瞬移、散花、最愛）
   - 按鈕文字（GO、STOP、加入目前位置）
   - 授權相關文字（剩餘免費次數、檢查授權、激活授權碼）
   - 設定模態框所有文字（語言、激活授權碼、購買授權、備份最愛地點）
   - 付款選擇模態框文字（選擇付款方案、年費方案、永久方案等）

#### 測試步驟
1. 打開 App
2. 點擊右下角 🔑 設定按鈕
3. 在語言下拉選單中選擇「English」
4. 確認以下元素是否已切換為英文：
   - 模式標籤（Teleport、Route、Favorites）
   - 按鈕文字（GO、STOP、Add Current Location）
   - 授權狀態文字（Remaining Free Trials）
   - 設定模態框標題和按鈕文字
   - 付款選擇模態框文字
5. 重複測試其他語言（日本語、한국어）
6. 確認語言選擇在重新啟動 App 後仍然有效

---

## 🏗️ 技術架構

### 前端（App）
- **框架：** SwiftUI + WKWebView
- **地圖：** Leaflet.js
- **通信：** WKScriptMessageHandler（Swift ↔ JavaScript）
- **本地儲存：** localStorage（最愛地點、語言選擇）

### 後端（服務器）
- **框架：** Express.js (Node.js)
- **部署平台：** Vercel
- **金流整合：** Polar SDK
- **數據存儲：** 內存 Map（生產環境建議使用數據庫）

### API 端點總覽

| 端點 | 方法 | 說明 |
|------|------|------|
| `/api/auth/check` | POST | 檢查授權狀態 |
| `/api/license/activate` | POST | 激活授權碼 |
| `/api/payment/create` | POST | 創建付款訂單 |
| `/api/payment/polar-webhook` | POST | Polar 付款回調 |
| `/payment` | GET | 付款選擇頁面 |
| `/payment/success` | GET | 付款成功頁面 |

---

## 🚀 部署狀態

### 網站部署
- **平台：** Vercel
- **URL：** `https://konggoo.tw`
- **狀態：** ✅ 已部署並運行正常

### App 打包
- **平台：** macOS
- **簽名方式：** Ad-hoc 簽名
- **打包腳本：** `build_dmg.sh`
- **輸出位置：** `website/downloads/ios-location-simulator-mac.dmg`

### 環境變數設定（Vercel）
- `POLAR_ACCESS_TOKEN`：Polar API 訪問令牌（可選）
- `POLAR_PRODUCT_PRICE_ID_ANNUAL`：年費方案價格 ID（可選）
- `POLAR_PRODUCT_PRICE_ID_LIFETIME`：永久方案價格 ID（可選）
- `ADMIN_KEY`：管理員密鑰（用於創建測試授權碼）

---

## ⚠️ 已知問題與解決方案

### 1. 語言切換後部分文字未更新
**狀態：** ✅ 已修復  
**解決方案：** 
- 確保所有 UI 元素都有 `data-i18n` 屬性
- 在 `applyTranslations()` 中強制重新渲染

### 2. 備份檔案與 App 資料不同步
**狀態：** ✅ 已修復  
**解決方案：**
- 添加自動備份功能（儲存最愛地點時自動備份）
- 添加手動備份按鈕（設定模態框中）
- 頁面載入時自動備份一次

### 3. 購買連結在設定中缺失
**狀態：** ✅ 已修復  
**解決方案：**
- 在設定模態框中添加「💳 購買授權」按鈕

### 4. 授權狀態顯示不一致
**狀態：** ✅ 已修復  
**解決方案：**
- 使用 `globalAuthState` 全局變數追蹤授權狀態
- 確保 UI 更新邏輯正確處理已激活狀態

---

## 📝 總結

### ✅ 已完成的功能
1. ✅ 完整的購買流程（從試用到付款到激活）
2. ✅ 授權系統（設備識別、試用管理、激活驗證）
3. ✅ 多語言支援（繁體中文、英文、日文、韓文）
4. ✅ App 內設置（語言切換、授權激活、購買連結、備份功能）
5. ✅ 自動備份功能（最愛地點自動備份到專案目錄）

### 🔄 待改進項目
1. ⚠️ 數據存儲：目前使用內存存儲，生產環境建議使用數據庫
2. ⚠️ 錯誤處理：可以添加更詳細的錯誤提示和日誌記錄
3. ⚠️ 測試覆蓋：可以添加自動化測試確保功能穩定性

### 📞 技術支援
如有任何問題，請檢查：
1. Xcode 控制台日誌（授權相關訊息）
2. 服務器日誌（Vercel 部署日誌）
3. 瀏覽器開發者工具（語言切換、UI 更新）

---

**報告結束**
