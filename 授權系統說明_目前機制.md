# 授權系統說明 - 目前機制

## 📋 設備識別機制

### 1. 設備 ID 獲取方式
目前使用 **Mac 硬體 UUID** 來識別每台電腦：

```swift
private func getDeviceId() -> String {
    // 獲取 Mac 硬體 UUID
    let output = shell("system_profiler SPHardwareDataType | grep 'Hardware UUID' | awk '{print $3}'")
    return output.trimmingCharacters(in: .whitespacesAndNewlines)
}
```

### 2. 設備 ID 特性
- ✅ **每台 Mac 都有唯一的 Hardware UUID**
- ✅ **UUID 不會改變**（除非更換主機板）
- ✅ **無法輕易偽造**（需要系統權限）
- ✅ **適合用於授權追蹤**

## 🔐 授權檢查流程

### 1. 檢查授權狀態 (`checkAuthStatus`)
1. 獲取設備 ID（Mac Hardware UUID）
2. 發送 POST 請求到服務器：`/api/auth/check`
3. 服務器返回：
   - `remaining`: 剩餘試用次數
   - `isPaid`: 是否已付費

### 2. 使用試用次數 (`useTrial`)
1. 獲取設備 ID
2. 發送 POST 請求到服務器：`/api/trial/use`
3. 服務器扣除一次試用次數
4. 返回更新後的剩餘次數

### 3. 激活授權碼 (`activateLicense`)
1. 獲取設備 ID
2. 發送 POST 請求到服務器：`/api/license/activate`
3. 服務器驗證授權碼並綁定到設備 ID
4. 設置 `isPaid = true`

## ⚠️ 安全機制

### 目前的安全措施
1. ✅ **服務器端驗證**：所有授權檢查都在服務器端進行
2. ✅ **設備綁定**：授權碼綁定到特定設備 ID
3. ✅ **無免費試用漏洞**：服務器無響應時不給免費試用（防止濫用）

### 潛在風險
1. ⚠️ **設備 ID 可能被偽造**（需要系統權限，但理論上可能）
2. ⚠️ **服務器端需要確保試用次數限制**（每台設備只有 3 次）

## 📊 數據流程

```
App 啟動
  ↓
獲取 Mac Hardware UUID
  ↓
發送 deviceId 到服務器
  ↓
服務器查詢該 deviceId 的記錄
  ↓
返回 remaining (0-3) 和 isPaid (true/false)
  ↓
App 顯示剩餘次數
```

## 🔍 需要確認的事項

1. **服務器端實現**：
   - 是否正確限制每台設備只有 3 次試用？
   - 是否正確綁定授權碼到設備 ID？
   - 是否有防止重複註冊的機制？

2. **設備 ID 唯一性**：
   - Mac Hardware UUID 是否足夠唯一？
   - 是否需要額外的識別方式？

3. **試用次數追蹤**：
   - 服務器如何存儲試用次數？
   - 是否有時間限制（例如：試用期過期）？
