# 🔒 安全檢查和激活流程總結

## ✅ 機密資料檢查結果

### 已處理的問題

1. **從 Git 追蹤中移除敏感文件**
   - ✅ 已移除 `website/Vercel環境變數設定步驟.md`
   - ✅ 已更新 `.gitignore`，排除所有包含敏感資訊的文件

2. **程式碼安全性**
   - ✅ `server.js` 使用環境變數，沒有硬編碼金鑰
   - ✅ `auth_server.js` 使用環境變數，沒有硬編碼金鑰
   - ✅ 所有 API 金鑰都從 `process.env` 讀取

### 目前狀態

**✅ 安全**：所有敏感資訊都儲存在 Vercel 環境變數中，不會上傳到 Git

**⚠️ 注意**：以下文件包含實際 API 金鑰，已加入 `.gitignore`，**不會被上傳**：
- `website/環境變數設定完成報告.md`
- `website/綠界API設定.txt`
- `website/設定Vercel環境變數詳細步驟.md`
- `website/Vercel環境變數設定步驟.md`
- `website/讓我幫您設定環境變數.md`
- 其他相關文件

---

## 💳 購買和激活流程

### 完整流程

```
1. 用戶下載 App
   ↓
2. 免費試用 3 次（自動註冊設備）
   ↓
3. 次數用完，App 提示購買
   ↓
4. 前往網站購買（https://konggoo.tw）
   ↓
5. 選擇付款方式（綠界/藍新）
   ↓
6. 完成付款
   ↓
7. 收到授權碼（付款成功頁面或 Email）
   ↓
8. 在 App 中輸入授權碼
   ↓
9. 激活成功，無限制使用
```

### 詳細操作步驟

#### 步驟 1：用戶在 App 中看到剩餘次數

- 打開 App → 點擊「授權」標籤
- 顯示：「🎁 剩餘免費次數: X 次」
- 當次數為 0 時，顯示紅色：「0 次（請購買授權）」

#### 步驟 2：購買流程

1. **訪問網站**
   - URL：`https://konggoo.tw`
   - 點擊「立即購買 NT$ 490」

2. **選擇付款方式**
   - 綠界科技（信用卡/ATM/超商）
   - 藍新金流（信用卡/ATM）

3. **完成付款**
   - 根據選擇的方式完成付款

#### 步驟 3：收到授權碼

**方式 1：付款成功頁面（立即）**
- 付款完成後自動跳轉
- 顯示授權碼：`PKM-XXXXXXXX`
- 提供下載連結

**方式 2：Email（如果實作）**
- 發送到用戶填寫的 Email
- 包含授權碼和使用說明

#### 步驟 4：在 App 中激活

1. **打開 App**
   - 進入「授權」頁面

2. **輸入授權碼**
   - 在「輸入授權碼」欄位輸入：`PKM-XXXXXXXX`
   - 點擊「✅ 激活授權碼」

3. **激活成功**
   - 顯示「✅ 授權激活成功！」
   - 剩餘次數變為「已購買，無限制使用」（綠色）
   - 可以無限制使用所有功能

---

## 🔧 技術實現

### 後端 API

1. **付款回調**：`POST /api/payment/return`
   - 驗證付款結果
   - 生成授權碼：`PKM-` + 16 位隨機字元
   - 儲存到 `licenses` Map

2. **激活授權碼**：`POST /api/license/activate`
   - 驗證授權碼
   - 綁定到設備（使用設備 UUID）
   - 更新設備狀態為 `isPaid: true`

### App 端

1. **檢查授權**：`AuthManager.checkAuth()`
   - 檢查設備是否已付費
   - 更新剩餘次數顯示

2. **激活授權碼**：`AuthManager.activateLicense()`
   - 發送設備 ID 和授權碼到後端
   - 更新本地授權狀態

---

## 📋 檢查清單

### 部署前檢查

- [x] 確認 `.gitignore` 已更新
- [x] 確認敏感文件不會被上傳
- [x] 確認所有 API 金鑰都在環境變數中
- [x] 確認付款流程正常運作
- [x] 確認激活流程正常運作

### 測試檢查

- [ ] 測試免費試用功能
- [ ] 測試剩餘次數顯示
- [ ] 測試付款流程（使用測試環境）
- [ ] 測試授權碼生成
- [ ] 測試授權碼激活
- [ ] 測試激活後的無限制使用

---

## ⚠️ 重要提醒

1. **永遠不要**將包含實際 API 金鑰的文件上傳到 Git
2. **定期檢查** Git 歷史，確認沒有意外上傳敏感資料
3. **使用環境變數**儲存所有敏感資訊
4. **測試環境**和**正式環境**使用不同的 API 金鑰

---

**最後更新**：2026-01-13
