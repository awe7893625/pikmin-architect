# Pikmin Architect 授權流程完整說明

## 📋 系統架構

您的系統有**兩個獨立的服務器**：

### 1. 授權服務器 (auth_server.js) - **必須一直運行**
- **位置**：`/server/auth_server.js`
- **端口**：`http://localhost:3000`
- **用途**：App 直接連接的服務器
- **功能**：
  - 設備註冊
  - 授權檢查
  - 試用次數扣減
  - 激活授權碼
- **狀態**：**必須一直運行**，App 才能正常工作

### 2. 網站服務器 (website/server.js) - **購買時才需要**
- **位置**：`/website/server.js`
- **端口**：`http://localhost:3001`（本地）或部署到 Vercel
- **用途**：購買頁面的服務器
- **功能**：
  - 創建付款訂單
  - 處理金流回調
  - 生成授權碼
  - 顯示付款成功頁面

---

## 🔄 完整授權流程

### 階段 1：用戶購買（網站）

```
1. 用戶訪問購買網站
   ↓
2. 點擊「立即購買」按鈕
   ↓
3. 選擇付款方式（信用卡/ATM/超商）
   ↓
4. 跳轉到金流平台（綠界/藍新）付款
   ↓
5. 付款成功後，金流平台回調網站服務器
   ↓
6. 網站服務器生成授權碼（格式：PKM-XXXXXXXX）
   ↓
7. 授權碼顯示在成功頁面：/payment/success?license=PKM-XXXXXXXX
   ↓
8. 授權碼也會發送到用戶 Email（需要配置 SMTP）
```

### 階段 2：用戶激活（App）

```
1. 用戶下載並打開 App
   ↓
2. App 自動連接到授權服務器 (localhost:3000)
   ↓
3. App 註冊設備 ID（自動生成，基於 Mac 硬體 UUID）
   ↓
4. 授權服務器返回：
   - 試用狀態：剩餘 3 次免費試用
   - 或已付費狀態（如果已激活）
   ↓
5. App 顯示剩餘次數在 UI 上
   ↓
6. 用戶使用功能時：
   - 每次使用前檢查授權
   - 試用用戶：扣減試用次數
   - 已付費用戶：直接允許
   ↓
7. 試用次數用完後，App 顯示激活 UI
   ↓
8. 用戶輸入授權碼（從購買成功頁面或 Email 獲取）
   ↓
9. App 發送激活請求到授權服務器
   ↓
10. 授權服務器驗證授權碼並綁定到設備
   ↓
11. 激活成功！用戶可以使用完整版功能
```

---

## ⚠️ 重要問題：授權碼同步

**目前發現的問題**：

付款成功後，授權碼在 `website/server.js` 中生成，但**沒有自動同步到 `auth_server.js`**！

### 解決方案

有兩種方式：

#### 方案 A：付款成功後自動同步（推薦）

修改 `website/server.js`，在生成授權碼後，自動調用 `auth_server.js` 的 API 創建授權碼：

```javascript
// 在 website/server.js 的付款成功回調中
app.post('/api/payment/return', async (req, res) => {
    // ... 現有代碼 ...
    
    // 生成授權碼
    const licenseKey = 'PKM-' + crypto.randomBytes(8).toString('hex').toUpperCase();
    
    // 同步到授權服務器
    try {
        await axios.post('http://localhost:3000/api/admin/create-license', {
            adminKey: process.env.ADMIN_KEY,
            licenseKey: licenseKey,
            paidAt: new Date().toISOString()
        });
    } catch (error) {
        console.error('同步授權碼到授權服務器失敗:', error);
    }
    
    // ... 其餘代碼 ...
});
```

#### 方案 B：手動同步（臨時方案）

付款成功後，手動在授權服務器中創建授權碼：

```bash
# 使用 curl 創建授權碼
curl -X POST http://localhost:3000/api/admin/create-license \
  -H "Content-Type: application/json" \
  -d '{
    "adminKey": "your-admin-key",
    "licenseKey": "PKM-XXXXXXXX"
  }'
```

---

## 🚀 啟動步驟

### 1. 啟動授權服務器（必須）

```bash
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本/server"
npm install  # 如果還沒安裝
npm start
```

**確認運行**：
- 終端顯示：`✅ 已載入 5 組測試激活碼`
- 終端顯示：`授權服務器運行在 http://localhost:3000`
- 訪問：`http://localhost:3000/api/auth/check` 應該有響應

### 2. 啟動網站服務器（可選，僅購買時需要）

```bash
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本/website"
npm install  # 如果還沒安裝
npm start
```

**確認運行**：
- 訪問：`http://localhost:3001` 應該看到購買頁面

### 3. 運行 App

在 Xcode 中運行 App，App 會自動連接到 `localhost:3000` 的授權服務器。

---

## 📱 App 中的授權流程

### 試用階段

1. **首次啟動**：
   - App 自動註冊設備 ID
   - 授權服務器返回：剩餘 3 次免費試用
   - UI 顯示：`🎁 剩餘免費次數: 3`

2. **使用功能**：
   - 點擊「GO」按鈕 → 檢查授權 → 扣減 1 次 → 顯示 `剩餘 2 次`
   - 點擊最愛的點 → 檢查授權 → 扣減 1 次 → 顯示 `剩餘 1 次`
   - 使用快速功能鍵 → 檢查授權 → 扣減 1 次 → 顯示 `剩餘 0 次`

3. **次數用完**：
   - UI 顯示：`❌ 剩餘免費次數: 0（請購買授權）`
   - 自動顯示激活 UI（輸入授權碼的輸入框）

### 激活階段

1. **輸入授權碼**：
   - 用戶從購買成功頁面或 Email 獲取授權碼
   - 在 App 的激活 UI 中輸入：`PKM-XXXXXXXX`
   - 點擊「✅ 激活授權碼」按鈕

2. **激活過程**：
   - App 發送請求到 `http://localhost:3000/api/license/activate`
   - 授權服務器驗證授權碼
   - 如果有效：綁定到設備，返回成功
   - 如果無效：返回錯誤訊息

3. **激活成功**：
   - UI 顯示：`✅ 授權激活成功！`
   - 剩餘次數顯示：`✅ 已付費（永久授權）`
   - 所有功能無限制使用

---

## 🔧 測試激活碼

授權服務器預先載入了 5 組測試激活碼：

1. `PKM-9A0EF1DD632BBC1D`
2. `PKM-CF711BA7A0DBC80C`
3. `PKM-2CA9C96C3A8AB6E8`
4. `PKM-FE6C8F8CB7F37339`
5. `PKM-F7EC3C6305756F6C`

**注意**：每個激活碼只能使用一次，綁定到一個設備。

---

## ❓ 常見問題

### Q1: 授權服務器需要一直開著嗎？

**A: 是的！** 授權服務器必須一直運行，App 才能：
- 檢查授權狀態
- 扣減試用次數
- 激活授權碼

如果授權服務器關閉，App 會無法連接，所有授權功能都會失效。

### Q2: 網站服務器需要一直開著嗎？

**A: 不需要。** 網站服務器只在用戶購買時才需要：
- 本地開發：可以按需啟動
- 生產環境：部署到 Vercel/Netlify，自動運行

### Q3: 付款完成後，App 怎麼知道？

**A: App 不會自動知道。** 流程是：
1. 用戶在網站付款成功
2. 網站顯示授權碼
3. 用戶手動在 App 中輸入授權碼激活

**未來可以改進**：
- 付款成功後，網站自動發送 Email
- Email 中包含「一鍵激活」連結（使用 deep link）
- 點擊連結自動打開 App 並填入授權碼

### Q4: 授權碼在哪裡生成？

**A: 目前有兩個地方生成授權碼**：

1. **網站服務器** (`website/server.js`)：
   - 付款成功後自動生成
   - 格式：`PKM-` + 16 位隨機十六進制

2. **授權服務器** (`server/auth_server.js`)：
   - 管理員手動創建（測試用）
   - 或通過 API 同步

**問題**：兩個服務器的授權碼沒有自動同步！

**解決**：需要在付款成功後，自動同步到授權服務器（見上方「授權碼同步」部分）。

---

## 🎯 總結

1. **授權服務器** (`localhost:3000`) → **必須一直運行**
2. **網站服務器** (`localhost:3001` 或 Vercel) → **購買時才需要**
3. **App** → 自動連接到授權服務器，不需要手動配置
4. **授權碼** → 付款成功後生成，用戶手動在 App 中激活
5. **試用次數** → 每次使用功能時自動扣減

**目前需要修復**：付款成功後，授權碼需要自動同步到授權服務器。
