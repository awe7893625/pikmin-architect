# 授權功能測試說明

## ✅ 已完成的修復

根據檢查，以下功能已經實現：

1. ✅ **授權檢查函數**：`checkAndUpdateAuthStatus()`、`activateLicense()`、`updateTrialStatusInWebView()`
2. ✅ **授權服務器**：正在運行在 `http://localhost:3000`
3. ✅ **測試激活碼**：已添加到服務器（5組）
4. ✅ **WebView 更新**：`index.html` 已更新，會顯示剩餘次數

## 🧪 測試步驟

### 1. 確認授權服務器運行
```bash
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本/server"
npm start
```

### 2. 在 Xcode 中運行 App
- 專案已打開：`Pikmin_Dev_Portable.xcodeproj`
- 按 ⌘R 運行 App

### 3. 測試授權功能

#### 測試 1：檢查剩餘次數顯示
- App 啟動後，應該在頂部看到「🎁 剩餘免費次數: 3」
- 如果沒有顯示，檢查瀏覽器控制台是否有錯誤

#### 測試 2：使用功能並消耗次數
1. 點擊地圖選擇一個位置
2. 點擊「GO」按鈕執行瞬移
3. 應該看到剩餘次數減少（3 → 2 → 1 → 0）
4. 每次使用後，剩餘次數應該自動更新

#### 測試 3：次數用完後鎖定
1. 使用 3 次後，剩餘次數應該顯示「0 次（請購買授權）」
2. 應該自動顯示激活 UI（輸入框和激活按鈕）
3. 再次點擊「GO」時，應該顯示錯誤提示：「❌ 試用次數已用完，請購買授權」

#### 測試 4：激活授權碼
1. 在激活 UI 中輸入測試激活碼：`PKM-9A0EF1DD632BBC1D`
2. 點擊「✅ 激活授權碼」按鈕
3. 應該顯示「✅ 授權激活成功！」
4. 剩餘次數應該顯示「已購買，無限制使用」
5. 現在可以無限制使用所有功能

## 🔍 故障排除

### 如果沒有顯示剩餘次數：
1. 打開瀏覽器開發者工具（在 App 中按 ⌥⌘I）
2. 檢查 Console 是否有 JavaScript 錯誤
3. 確認 `updateTrialStatus` 函數是否被調用
4. 檢查 `AuthManager` 的 API 連接是否正常

### 如果次數沒有減少：
1. 檢查 `userContentController` 中的 `tp` 和 `startRoute` case 是否有 `performActionWithAuth` 調用
2. 查看 Xcode 控制台是否有錯誤訊息
3. 確認授權服務器的 `/api/trial/use` API 是否正常

### 如果激活碼無法使用：
1. 確認激活碼格式正確（`PKM-` 開頭）
2. 檢查該激活碼是否已被使用（每個只能使用一次）
3. 查看授權服務器控制台的錯誤訊息

## 📊 測試激活碼列表

每個激活碼只能使用一次，綁定到設備 ID：

1. `PKM-9A0EF1DD632BBC1D`
2. `PKM-CF711BA7A0DBC80C`
3. `PKM-2CA9C96C3A8AB6E8`
4. `PKM-FE6C8F8CB7F37339`
5. `PKM-F7EC3C6305756F6C`

## 🎯 預期行為

### 首次啟動
- 顯示「剩餘免費次數: 3」
- 激活 UI 隱藏

### 使用 1-2 次
- 剩餘次數減少（3 → 2 → 1）
- 激活 UI 仍然隱藏
- 功能正常使用

### 使用 3 次後
- 顯示「0 次（請購買授權）」
- 激活 UI 自動顯示
- 再次使用功能時顯示錯誤提示

### 激活成功後
- 顯示「已購買，無限制使用」
- 激活 UI 隱藏
- 可以無限制使用所有功能
