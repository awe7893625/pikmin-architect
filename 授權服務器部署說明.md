# 授權服務器部署說明 - 用戶不需要開任何東西！

## 🎯 重要澄清

**用戶購買授權時，完全不需要開任何本地服務！**

### ❌ 誤解
- ~~用戶需要開著 `localhost:3000`~~
- ~~用戶需要運行授權服務器~~

### ✅ 正確理解

1. **開發環境**（您自己測試時）：
   - `localhost:3000` 只是給您本地測試用的
   - 您自己運行 `npm start` 來測試

2. **生產環境**（用戶使用時）：
   - 授權服務器部署到雲端（Vercel、Railway、Render）
   - 用戶完全不需要開任何東西
   - 用戶只需要：
     - 訪問購買網站（已部署在雲端）
     - 付款
     - 獲取授權碼
     - 在 App 中激活

---

## 📋 完整流程（用戶視角）

```
用戶購買流程（完全不需要開任何本地服務）：

1. 用戶訪問購買網站
   ↓ （網站已部署在 Vercel/Netlify，24/7 運行）
2. 點擊「立即購買」→ 選擇付款方式
   ↓
3. 跳轉到金流平台付款
   ↓
4. 付款成功後，網站服務器生成授權碼
   ↓ （網站服務器自動同步授權碼到雲端授權服務器）
5. 顯示成功頁面，授權碼顯示給用戶
   ↓
6. 用戶下載 App（已編譯，內含授權服務器 URL）
   ↓
7. 用戶在 App 中輸入授權碼
   ↓ （App 連接到雲端授權服務器驗證）
8. 激活成功！
```

**用戶完全不需要開任何本地服務！**

---

## 🚀 部署步驟（您需要做的）

### 步驟 1：部署授權服務器到雲端

選擇一個免費平台：

#### 選項 A：Vercel（推薦）

```bash
cd server
npm install -g vercel
vercel login
vercel --prod
```

部署成功後，會得到一個 URL，例如：
- `https://pikmin-auth-server.vercel.app`

#### 選項 B：Railway

1. 前往 https://railway.app/
2. 註冊帳號（可用 GitHub 登入）
3. 點擊 "New Project" → "Deploy from GitHub repo"
4. 選擇您的 `server/` 目錄
5. 設定環境變數：`ADMIN_KEY`

#### 選項 C：Render

1. 前往 https://render.com/
2. 註冊帳號
3. 點擊 "New" → "Web Service"
4. 連接 GitHub repo，選擇 `server/` 目錄
5. 設定環境變數：`ADMIN_KEY`

### 步驟 2：更新 App 代碼

在 `AuthManager.swift` 中，已經自動支持：

- **開發環境**：自動使用 `localhost:3000`
- **生產環境**：需要設定雲端 URL

修改 `AuthManager.swift` 中的生產環境 URL：

```swift
#else
// 生產環境：改為您部署的授權服務器地址
return "https://pikmin-auth-server.vercel.app/api" // ⚠️ 改為您的實際 URL
#endif
```

### 步驟 3：更新網站服務器

在 `website/server.js` 中，已經有同步功能：

```javascript
const authServerUrl = process.env.AUTH_SERVER_URL || 'http://localhost:3000';
```

部署網站到 Vercel/Netlify 時，設定環境變數：
- `AUTH_SERVER_URL` = `https://pikmin-auth-server.vercel.app`

### 步驟 4：編譯 App

在 Xcode 中：
1. 選擇 Release 配置
2. Archive → Export
3. 打包為 .dmg 或 .app

App 會自動連接到您設定的雲端授權服務器。

---

## ✅ 總結

### 開發環境（您自己測試）
- 授權服務器：`localhost:3000`（本地運行）
- 網站服務器：`localhost:3001`（本地運行）
- App：連接到 `localhost:3000`

### 生產環境（用戶使用）
- 授權服務器：部署到雲端（Vercel/Railway/Render）
- 網站服務器：部署到雲端（Vercel/Netlify）
- App：連接到雲端授權服務器 URL

### 用戶端
- ✅ **不需要開任何東西**
- ✅ 只需要訪問購買網站、付款、獲取授權碼、在 App 中激活

---

## 🎯 快速檢查清單

- [ ] 部署授權服務器到 Vercel/Railway/Render
- [ ] 獲取授權服務器 URL
- [ ] 更新 `AuthManager.swift` 中的生產環境 URL
- [ ] 更新 `website/server.js` 的 `AUTH_SERVER_URL` 環境變數
- [ ] 部署網站服務器到 Vercel/Netlify
- [ ] 編譯 App（Release 模式）
- [ ] 測試完整流程

完成後，用戶就可以正常購買和激活，完全不需要開任何本地服務！
