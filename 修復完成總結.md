# 授權系統修復完成總結

## ✅ 已修復的問題

### 1. App UI 顯示剩餘免費次數
- ✅ 修改 `index.html` 中的 `trial-status-bar`，將 `display` 從 `none` 改為 `block`
- ✅ 添加 `DOMContentLoaded` 事件監聽器，在頁面載入時自動檢查授權狀態
- ✅ 添加 `updateTrialStatus` JavaScript 函數，正確更新剩餘次數顯示

### 2. App 在3次使用後鎖定
- ✅ 在 `ContentView.swift` 的 `userContentController` 中，為 `tp` 和 `startRoute` 操作添加授權檢查
- ✅ 使用 `authManager.performActionWithAuth` 來檢查和使用試用次數
- ✅ 當試用次數用完時，顯示錯誤提示並阻止操作

### 3. 激活碼輸入和付款頁面資訊
- ✅ 添加 `checkAuth` 和 `activateLicense` 消息處理
- ✅ 添加 `checkAndUpdateAuthStatus`、`activateLicense` 和 `updateTrialStatusInWebView` Swift 函數
- ✅ 在 `index.html` 中，當剩餘次數為 0 時自動顯示激活 UI

### 4. Python 路徑管理
- ✅ 使用 `PythonManager.shared` 來動態檢測 Python 路徑
- ✅ 替換所有硬編碼的 `pythonPath` 為 `getPythonPath()` 方法
- ✅ 確保自動安裝 `pymobiledevice3` 的功能正常工作

### 5. 授權狀態同步
- ✅ 在 `init()` 中訂閱 `AuthManager` 的 `authStatus` 和 `remainingTrial` 變化
- ✅ 當授權狀態變化時，自動更新 WebView 中的顯示
- ✅ 在 App 啟動時自動檢查授權狀態

## 📝 修改的文件

1. **`PikminArchitect/ContentView.swift`**
   - 添加 `PythonManager` 和 `cancellables` 屬性
   - 添加 `getPythonPath()` 方法
   - 修改 `init()` 方法，添加授權狀態訂閱
   - 修改 `userContentController`，添加授權檢查
   - 添加 `checkAndUpdateAuthStatus()`、`activateLicense()` 和 `updateTrialStatusInWebView()` 方法
   - 替換所有 `self.pythonPath` 為 `self.getPythonPath()`

2. **`PikminArchitect/index.html`**
   - 修改 `trial-status-bar` 的 `display` 為 `block`
   - 添加 `DOMContentLoaded` 事件監聽器
   - 確保 `updateTrialStatus` 函數正確控制激活 UI 的顯示

## 🧪 測試激活碼

已添加到 `auth_server.js` 的測試激活碼（每個只能使用一次）：
1. `PKM-9A0EF1DD632BBC1D`
2. `PKM-CF711BA7A0DBC80C`
3. `PKM-2CA9C96C3A8AB6E8`
4. `PKM-FE6C8F8CB7F37339`
5. `PKM-F7EC3C6305756F6C`

## 🚀 啟動步驟

### 1. 啟動授權服務器
```bash
cd "/Users/rain/Pikmin_Dev_Portable/PikminArchitect＿繼續開發版本/server"
npm start
```

服務器應該運行在 `http://localhost:3000`

### 2. 在 Xcode 中運行 App
- 打開 Xcode 專案
- 選擇目標設備（Mac）
- 點擊運行按鈕（⌘R）

### 3. 測試流程
1. **首次啟動**：App 應該顯示「剩餘免費次數: 3」
2. **使用瞬移或散花功能**：每次使用後，剩餘次數應該減少
3. **使用3次後**：應該顯示「0 次（請購買授權）」，並自動顯示激活 UI
4. **輸入激活碼**：使用上述測試激活碼之一，應該成功激活並顯示「✅ 授權激活成功！」

## ⚠️ 注意事項

1. **授權服務器必須運行**：App 需要連接到 `http://localhost:3000` 的授權服務器
2. **每個激活碼只能使用一次**：激活碼會綁定到設備 ID，不能重複使用
3. **設備 ID**：App 使用 Mac 硬體 UUID + Bundle ID 生成唯一設備 ID
4. **試用次數**：每個設備有 3 次免費試用機會

## 🔍 故障排除

### 如果 App 沒有顯示剩餘次數：
1. 檢查授權服務器是否正在運行
2. 檢查 `AuthManager.swift` 中的 `apiBaseURL` 是否正確（應該是 `http://localhost:3000/api`）
3. 查看 Xcode 控制台是否有錯誤訊息

### 如果激活碼無法使用：
1. 確認激活碼格式正確（`PKM-` 開頭，16 位十六進制）
2. 檢查授權服務器的控制台，查看是否有錯誤
3. 確認該激活碼尚未被其他設備使用

### 如果 Python 路徑錯誤：
1. 確認已安裝 Python 3
2. 檢查 `PythonManager.swift` 中的路徑檢測邏輯
3. 查看 Xcode 控制台是否有 Python 相關錯誤

## 📊 授權服務器 API

- `POST /api/device/register` - 註冊設備
- `POST /api/auth/check` - 檢查授權狀態
- `POST /api/trial/use` - 使用試用次數
- `POST /api/license/activate` - 激活授權碼

所有 API 都需要 `deviceId` 參數。
