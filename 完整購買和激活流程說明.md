# 完整購買和激活流程說明

## 📋 功能需求總覽

### 1. 授權系統
- **免費試用**：每個設備（Mac Hardware UUID）有 3 次免費試用
- **年費方案**：NT$ 690，1 年授權，無限使用
- **永久方案**：NT$ 1,750，永久授權，無限使用
- **激活碼限制**：每個激活碼只能使用一次（綁定一個設備）

### 2. UI 顯示規則
- **已激活用戶**：完全隱藏「剩餘免費次數」顯示，不顯示任何授權相關 UI
- **試用用戶（還有次數）**：顯示「剩餘免費次數: X次」，隱藏購買 UI
- **試用用戶（次數用完）**：顯示「剩餘免費次數: 0次（請購買授權）」，顯示購買按鈕和激活碼輸入框

### 3. 購買流程
- 用戶點擊「購買授權」按鈕
- 跳轉到付款頁面（Vercel 或本地服務器）
- 選擇方案（年費或永久）
- 完成付款（Polar 金流或模擬付款）
- 獲得授權碼
- 回到 App 輸入激活碼

### 4. 激活流程
- 用戶在 App 設定中輸入授權碼
- App 發送激活請求到服務器
- 服務器驗證授權碼並綁定設備
- App UI 更新為已激活狀態（隱藏所有試用次數顯示）

---

## 🔄 完整流程步驟

### 步驟 1：用戶打開 App
1. App 啟動時自動檢查授權狀態
2. 每 5 秒自動檢查一次授權狀態（確保 UI 同步）
3. 根據授權狀態顯示對應的 UI

### 步驟 2：用戶點擊「購買授權」
**觸發位置**：
- 設定模態框中的「💰 購買授權」按鈕
- 或授權 UI 中的「購買授權」按鈕（當試用次數為 0 時）

**執行流程**：
1. App 調用 `openPaymentLink()` 函數
2. 打開外部瀏覽器，跳轉到：`https://konggoo.tw/payment`
3. 如果 Vercel 部署失敗，使用本地服務器：`http://localhost:3001/payment`

### 步驟 3：選擇付款方案
**付款頁面** (`/payment`)：
- 顯示兩個方案：
  - **年費方案**：NT$ 690（推薦）
  - **永久方案**：NT$ 1,750
- 用戶選擇方案
- 點擊「前往付款」按鈕

**後端處理** (`POST /api/payment/create`)：
1. 創建訂單（狀態：pending）
2. 生成訂單 ID（格式：`ORD-{timestamp}-{random}`）
3. 檢查是否有 Polar 金流設定：
   - **有 Polar 設定**：創建 Polar checkout session，返回 Polar 付款 URL
   - **無 Polar 設定**：返回模擬付款頁面 URL（`/payment/process?orderId={orderId}`）

### 步驟 4：完成付款

#### 4.1 使用 Polar 金流（生產環境）
1. 跳轉到 Polar 付款頁面
2. 用戶完成付款
3. Polar 發送 webhook 到：`POST /api/payment/polar-webhook`（以 `https://konggoo.tw` 為主域名）
4. 服務器生成授權碼（格式：`PKM-{16位hex}`）
5. 更新訂單狀態為 `paid`
6. 將授權碼存儲到 `licenses` Map
7. 跳轉到成功頁面：`/payment/success?licenseKey={licenseKey}&orderId={orderId}`

#### 4.2 使用模擬付款（測試環境）
1. 跳轉到模擬付款頁面：`/payment/process?orderId={orderId}`
2. 顯示訂單信息（訂單編號、方案、金額）
3. 用戶點擊「確認付款（測試）」按鈕
4. 調用 `POST /api/payment/confirm`
5. 服務器生成授權碼
6. 更新訂單狀態為 `paid`
7. 跳轉到成功頁面：`/payment/success?licenseKey={licenseKey}&orderId={orderId}`

### 步驟 5：查看授權碼
**成功頁面** (`/payment/success`)：
- 顯示「付款成功！」訊息
- 顯示授權碼（格式：`PKM-XXXXXXXX`）
- 提供「複製授權碼」按鈕
- 顯示使用說明：
  1. 打開「摃菇 iOS 模擬器」App
  2. 點擊右下角的「⚙️」設定按鈕
  3. 在「激活授權碼」欄位中貼上授權碼
  4. 點擊「✅ 激活授權碼」按鈕

### 步驟 6：在 App 中激活授權碼
**App 設定模態框**：
1. 用戶點擊右下角「⚙️」設定按鈕
2. 在「激活授權碼」輸入框中輸入授權碼
3. 點擊「✅ 激活授權碼」按鈕

**App 處理流程**：
1. App 調用 `activateLicenseFromButton()` 函數
2. 通過 WebKit bridge 發送消息：`{ action: 'activateLicense', licenseKey: 'PKM-...' }`
3. Swift 的 `ContentView.swift` 接收消息，調用 `activateLicense(licenseKey:)` 函數
4. 發送 `POST /api/license/activate` 請求到服務器
5. 服務器驗證：
   - 授權碼是否存在
   - 授權碼是否有效
   - 授權碼是否已被其他設備使用（每個激活碼只能使用一次）
6. 如果驗證通過：
   - 綁定設備到授權碼
   - 更新設備狀態為已激活
   - 返回成功響應
7. App 收到成功響應後：
   - 清除本地存儲（`localStorage.removeItem('favLocs')` 等）
   - 強制更新 UI 為已激活狀態
   - 隱藏所有「剩餘免費次數」顯示
   - 顯示成功訊息
   - 1.5 秒後重新檢查授權狀態（確保同步）

### 步驟 7：使用已激活的 App
**激活後的 UI 狀態**：
- ✅ 頂部「剩餘免費次數」完全隱藏（`display: none`, `visibility: hidden`, `height: 0`）
- ✅ 授權 UI 完全隱藏
- ✅ 所有功能無限制使用（瞬移、散花、最愛）
- ✅ 設定中仍可購買升級方案（年費用戶可升級到永久）

**功能使用**：
- **瞬移**：點擊地圖任意位置，立即移動到該位置
- **散花**：選擇路徑點，沿路徑移動（模擬真實移動）
- **最愛**：保存常用地點，快速跳轉

---

## 🔧 技術實現細節

### 1. 授權狀態檢查
- **觸發時機**：
  - App 啟動時
  - 每 5 秒自動檢查
  - 手動點擊「檢查授權」按鈕
  - 激活授權碼後

- **檢查流程**：
  1. App 發送 `POST /api/auth/check` 請求
  2. 服務器返回：`{ trialCount, licenseKey, isActivated }`
  3. App 根據 `isActivated` 更新 UI：
     - `isActivated: true` → `updateTrialStatus(999, true)` → 隱藏所有試用次數顯示
     - `isActivated: false` → `updateTrialStatus(trialCount, false)` → 顯示試用次數

### 2. 購買連結處理
- **URL 生成**：
  ```javascript
  const paymentUrl = `https://konggoo.tw/payment?planType=${planType}`;
  ```

- **打開方式**：
  - 優先使用 WebKit bridge：`window.webkit.messageHandlers.bridge.postMessage({ action: 'openPayment', url: paymentUrl })`
  - 回退到 `window.open(paymentUrl, '_blank')`

### 3. 授權碼生成
- **格式**：`PKM-{16位大寫hex}`
- **生成時機**：付款成功後（Polar webhook 或模擬付款確認）
- **存儲**：存儲在服務器的 `licenses` Map 中

### 4. 激活碼驗證
- **驗證項目**：
  1. 授權碼是否存在
  2. 授權碼是否有效（`isValid: true`）
  3. 授權碼是否已被使用（`deviceId` 是否為空或與當前設備匹配）

- **綁定規則**：
  - 每個激活碼只能綁定一個設備（`deviceId`）
  - 如果已綁定其他設備，返回錯誤：「授權碼已被其他設備使用，每個激活碼只能使用一次」

---

## 🐛 當前問題和修復

### 問題 1：購買連結無法打開
**原因**：Vercel 路由配置問題
**修復**：
- 已修復 `vercel.json` 路由配置
- 已添加 `/payment` 路由優先級
- 已使用 `path.resolve` 確保文件路徑正確

### 問題 2：激活後 UI 仍顯示「剩餘免費次數: 3次」
**原因**：UI 更新邏輯問題
**修復**：
- 添加定期檢查（每 5 秒）
- 修復 `checkAuthStatus` 函數（使用 `action` 而不是 `act`）
- 強制隱藏頂部狀態欄（多種 CSS 屬性）

### 問題 3：授權狀態不同步
**原因**：檢查頻率不足
**修復**：
- 添加 `setInterval` 每 5 秒自動檢查
- 激活後立即檢查並更新 UI

---

## 📝 部署檢查清單

### Vercel 環境變數
- [ ] `POLAR_ACCESS_TOKEN`：從 Polar 後台獲取
- [ ] `POLAR_PRODUCT_PRICE_ID_ANNUAL`：`3c450333-7ba3-4d6a-a93a-b4cb0a9b8aa2`（已有默認值）
- [ ] `POLAR_PRODUCT_PRICE_ID_LIFETIME`：`4e7ccc7a-c4ef-4f55-b296-5c0f81d0af60`（已有默認值）
- [ ] `SUCCESS_URL`：`https://konggoo.tw/payment/success`（可選）

### Polar Webhook 設定
- [ ] 在 Polar 後台設定 webhook URL：`https://konggoo.tw/api/payment/polar-webhook`
- [ ] 選擇事件：`checkout.succeeded`

---

## 🧪 測試流程

### 測試 1：免費試用
1. 打開 App（新設備）
2. 應該顯示「剩餘免費次數: 3次」
3. 使用「瞬移」或「散花」功能
4. 每次使用後，次數應該減少
5. 當次數為 0 時，應該顯示購買按鈕

### 測試 2：購買流程
1. 點擊「購買授權」按鈕
2. 應該打開付款頁面
3. 選擇方案（年費或永久）
4. 完成付款（Polar 或模擬）
5. 應該看到授權碼

### 測試 3：激活流程
1. 複製授權碼
2. 在 App 設定中輸入授權碼
3. 點擊「激活授權碼」
4. 應該看到成功訊息
5. 頂部「剩餘免費次數」應該完全隱藏
6. 所有功能應該無限制使用

### 測試 4：激活後 UI
1. 重新啟動 App
2. 應該自動檢查授權狀態
3. 頂部「剩餘免費次數」應該保持隱藏
4. 設定中仍可購買升級方案

---

## 📞 故障排除

### 問題：購買連結無法打開
**解決方案**：
1. 檢查 Vercel 部署狀態
2. 等待 1-2 分鐘讓 Vercel 完成部署
3. 檢查瀏覽器控制台錯誤
4. 嘗試使用本地服務器：`http://localhost:3001/payment`

### 問題：激活後仍顯示「剩餘免費次數: 3次」
**解決方案**：
1. 等待 5 秒讓自動檢查生效
2. 手動點擊「檢查授權」按鈕
3. 重新啟動 App
4. 檢查 Xcode 控制台日誌

### 問題：授權碼無法激活
**解決方案**：
1. 檢查授權碼格式（應為 `PKM-{16位hex}`）
2. 檢查授權碼是否已被使用
3. 檢查服務器日誌
4. 檢查網絡連接

---

## 🎯 總結

### 核心功能
1. ✅ 免費試用系統（每個設備 3 次）
2. ✅ 購買授權系統（年費 NT$ 690，永久 NT$ 1,750）
3. ✅ Polar 金流整合（自動檢測環境變數）
4. ✅ 激活碼系統（每個激活碼只能使用一次）
5. ✅ UI 自動更新（每 5 秒檢查授權狀態）

### 用戶體驗
1. ✅ 清晰的購買流程
2. ✅ 簡單的激活流程
3. ✅ 自動 UI 同步
4. ✅ 完整的錯誤處理

### 技術實現
1. ✅ 安全的授權驗證
2. ✅ 可靠的狀態同步
3. ✅ 靈活的金流整合
4. ✅ 完善的錯誤處理
