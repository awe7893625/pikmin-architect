# 🚀 Pikmin Architect 快速部署指南

## 5 分鐘快速部署到 Vercel

### 步驟 1：準備環境變數

1. 複製 `.env.example` 為 `.env`：
```bash
cd website
cp .env.example .env
```

2. 編輯 `.env` 文件，填入您的金流 API 金鑰

### 步驟 2：安裝 Vercel CLI

```bash
npm i -g vercel
```

### 步驟 3：登入 Vercel

```bash
vercel login
```

### 步驟 4：部署

```bash
cd website
vercel
```

按照提示操作：
- 是否要設置項目？**Y**
- 項目名稱？**pikmin-architect**（或自訂）
- 是否要覆蓋設置？**N**（首次部署）

### 步驟 5：設定環境變數

1. 前往 https://vercel.com/dashboard
2. 選擇您的項目
3. 前往 Settings → Environment Variables
4. 添加所有 `.env` 中的變數

### 步驟 6：重新部署

```bash
vercel --prod
```

### 完成！🎉

您的網站現在應該運行在：`https://your-project.vercel.app`

---

## 部署授權服務器

授權服務器需要單獨部署（因為需要不同的端口）。

### 選項 1：Railway（推薦）

1. 前往 https://railway.app/
2. 連接 GitHub
3. 導入 `server` 目錄
4. 設定環境變數
5. 部署完成

### 選項 2：Heroku

```bash
cd server
heroku create pikmin-architect-auth
git push heroku main
```

### 選項 3：同一個 Vercel 項目（使用 API Routes）

可以將 `auth_server.js` 整合到 `website/server.js` 中，使用不同的路由。

---

## 測試部署

### 1. 測試網站

訪問您的 Vercel URL，確認網站正常顯示。

### 2. 測試付款流程

1. 點擊「立即購買」
2. 選擇付款方式
3. 使用測試卡號完成付款
4. 確認授權碼生成

### 3. 測試授權驗證

1. 在 App 中輸入授權碼
2. 確認授權成功

---

## 常見問題

### Q: 部署後無法訪問？
**A:** 檢查環境變數是否正確設定，特別是 `PORT` 和 `NODE_ENV`。

### Q: 付款回調失敗？
**A:** 確認 `ECPAY_RETURN_URL` 和 `ECPAY_ORDER_RESULT_URL` 設定為正確的 Vercel URL。

### Q: 授權服務器無法連接？
**A:** 確認 `AUTH_SERVER_URL` 設定為授權服務器的正確 URL。

---

## 下一步

1. ✅ 部署完成
2. ⬜ 設定自訂域名
3. ⬜ 申請 SSL 證書（Vercel 自動提供）
4. ⬜ 設定數據庫（可選）
5. ⬜ 整合 Email 功能（可選）

---

## 需要幫助？

查看：
- `網站部署狀態報告.md` - 完整狀態報告
- `完整購買授權金流指南.md` - 詳細流程說明
- `網站部署指南.md` - 其他部署選項
