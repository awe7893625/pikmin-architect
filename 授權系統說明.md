# Pikmin Architect 授權系統說明

## 系統架構

### 1. 後端服務器 (`server/auth_server.js`)
- 管理設備註冊和授權驗證
- 追蹤試用次數（每個設備 3 次免費）
- 管理付費授權碼
- 防止重複下載（設備綁定）

### 2. macOS App (`PikminArchitect/AuthManager.swift`)
- 設備指紋識別（Mac 硬體 UUID）
- 自動註冊設備
- 檢查授權狀態
- 試用次數管理
- 授權碼激活

### 3. 下載頁面 (`download_page.html`)
- 提供試用版下載
- 付費流程
- 授權碼輸入

## 使用流程

### 用戶端
1. **首次使用**：
   - 下載並啟動 App
   - App 自動註冊設備（使用 Mac 硬體 UUID）
   - 獲得 3 次免費試用

2. **試用期間**：
   - 每次使用核心功能（瞬移、路線、真實軌跡）會消耗 1 次試用
   - 剩餘次數顯示在「授權」標籤頁

3. **購買授權**：
   - 試用次數用完後，需要購買授權碼
   - 在「授權」標籤頁輸入授權碼
   - 激活後可無限制使用

### 管理員端
1. **創建授權碼**：
```bash
curl -X POST http://localhost:3000/api/admin/create-license \
  -H "Content-Type: application/json" \
  -d '{"adminKey": "your-secret-admin-key", "deviceId": "optional-device-id"}'
```

2. **查看統計**：
```bash
curl "http://localhost:3000/api/admin/stats?adminKey=your-secret-admin-key"
```

## 部署步驟

### 1. 啟動後端服務器
```bash
cd server
npm install
export ADMIN_KEY=your-secret-key
export PORT=3000
npm start
```

### 2. 配置 App
在 `AuthManager.swift` 中修改 `apiBaseURL`：
```swift
private let apiBaseURL = "https://your-server.com/api"
```

### 3. 將 AuthManager.swift 加入 Xcode 項目
1. 在 Xcode 中右鍵點擊項目
2. 選擇 "Add Files to..."
3. 選擇 `AuthManager.swift`
4. 確保勾選 "Copy items if needed" 和目標

### 4. 編譯和打包
- 正常編譯項目
- 使用 Archive 功能打包為 .dmg 或 .app

## API 端點說明

### `/api/device/register`
註冊新設備，返回試用次數和授權狀態

### `/api/auth/check`
檢查當前設備的授權狀態

### `/api/trial/use`
使用一次試用次數

### `/api/license/activate`
激活授權碼，綁定到當前設備

### `/api/admin/create-license`
管理員創建新的授權碼

### `/api/admin/stats`
查看設備和授權統計

## 安全注意事項

1. **生產環境建議**：
   - 使用 HTTPS
   - 使用數據庫（MongoDB/PostgreSQL）替代內存 Map
   - 添加日誌記錄
   - 使用更安全的設備識別方法
   - 添加 API 速率限制

2. **設備綁定**：
   - 當前使用 Mac 硬體 UUID + Bundle ID
   - 可考慮加入更多設備特徵（序列號、MAC 地址等）

3. **授權碼安全**：
   - 授權碼應使用更複雜的生成算法
   - 可添加過期時間
   - 限制每個授權碼的使用次數

## 測試

### 測試試用功能
1. 啟動 App
2. 嘗試使用「瞬移」功能
3. 檢查「授權」標籤頁，應顯示剩餘試用次數

### 測試授權激活
1. 創建授權碼（使用管理員 API）
2. 在 App 的「授權」標籤頁輸入授權碼
3. 驗證激活成功

## 付款整合建議

可以整合以下付款系統：
- **綠界科技**（台灣）
- **Stripe**（國際）
- **PayPal**
- **Apple In-App Purchase**（如果上架 App Store）

付款成功後，自動調用 `/api/admin/create-license` 創建授權碼並發送給用戶。
