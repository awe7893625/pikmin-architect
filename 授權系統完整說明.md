# 授權系統完整說明

## 📋 目前機制總覽

### 1. 設備識別方式
**使用 Mac Hardware UUID** 來識別每台電腦：

```swift
private func getDeviceId() -> String {
    // 獲取 Mac 硬體 UUID
    let output = shell("system_profiler SPHardwareDataType | grep 'Hardware UUID' | awk '{print $3}'")
    return output.trimmingCharacters(in: .whitespacesAndNewlines)
}
```

**特性：**
- ✅ 每台 Mac 都有唯一的 Hardware UUID
- ✅ UUID 不會改變（除非更換主機板）
- ✅ 無法輕易偽造（需要系統權限）
- ✅ **每台電腦的 UUID 都不同，確保不能共用免費試用**

### 2. 授權檢查流程

#### 步驟 1: App 啟動時檢查授權
```swift
func checkAuthStatus() {
    let deviceId = getDeviceId()  // 獲取這台 Mac 的 UUID
    // 發送 POST 請求到服務器：/api/auth/check
    // 服務器返回：remaining (0-3) 和 isPaid (true/false)
}
```

#### 步驟 2: 使用功能時扣除次數
```swift
func useTrial() {
    let deviceId = getDeviceId()  // 使用同一台 Mac 的 UUID
    // 發送 POST 請求到服務器：/api/trial/use
    // 服務器扣除一次試用次數，返回更新後的 remaining
}
```

#### 步驟 3: 激活授權碼
```swift
func activateLicense(licenseKey: String) {
    let deviceId = getDeviceId()  // 使用同一台 Mac 的 UUID
    // 發送 POST 請求到服務器：/api/license/activate
    // 服務器驗證授權碼並綁定到這台 Mac 的 UUID
}
```

## 🔐 安全機制

### ✅ 已實現的安全措施

1. **設備綁定**
   - 每台 Mac 的 UUID 是唯一的
   - 授權碼綁定到特定設備 ID
   - **無法在不同電腦間共用免費試用**

2. **服務器端驗證**
   - 所有授權檢查都在服務器端進行
   - 客戶端無法偽造授權狀態
   - 試用次數由服務器端控制

3. **防止濫用**
   - 服務器無響應時不給免費試用
   - 必須連接到服務器才能使用
   - 無法離線使用免費試用

### ⚠️ 需要確認的服務器端實現

**關鍵問題：服務器端是否正確限制每台設備只有 3 次試用？**

需要確認：
1. ✅ 服務器是否正確存儲每台設備的試用次數？
2. ✅ 服務器是否在首次註冊時給予 3 次試用？
3. ✅ 服務器是否正確扣除試用次數？
4. ✅ 服務器是否防止重複註冊（同一 deviceId 多次註冊）？

## 📊 數據流程圖

```
用戶打開 App
  ↓
獲取 Mac Hardware UUID (例如: "A1B2C3D4-E5F6-7890-ABCD-EF1234567890")
  ↓
發送 deviceId 到服務器: POST /api/auth/check
  ↓
服務器查詢數據庫：
  - 如果 deviceId 不存在 → 創建新記錄，remaining = 3
  - 如果 deviceId 存在 → 返回現有的 remaining 和 isPaid
  ↓
App 顯示剩餘次數
  ↓
用戶使用功能（瞬移/散花）
  ↓
App 調用 useTrial()
  ↓
發送 deviceId 到服務器: POST /api/trial/use
  ↓
服務器：
  - 檢查 remaining > 0
  - 扣除 1 次：remaining = remaining - 1
  - 返回更新後的 remaining
  ↓
App 更新顯示
```

## 🛡️ 防止濫用的機制

### 1. 設備 ID 唯一性
- Mac Hardware UUID 是硬體層級的識別碼
- 每台 Mac 都有不同的 UUID
- **無法在不同電腦間共用免費試用**

### 2. 服務器端控制
- 所有試用次數都在服務器端管理
- 客戶端無法修改試用次數
- 必須連接到服務器才能使用

### 3. 授權碼綁定
- 授權碼綁定到特定設備 ID
- 無法在不同設備間轉移
- 每台設備需要獨立的授權碼

## ⚠️ 潛在風險與防護

### 風險 1: 設備 ID 偽造
**風險等級：低**
- 需要系統管理員權限
- 需要修改系統配置
- 一般用戶無法做到

**防護措施：**
- 可以考慮添加額外的設備識別方式（例如：MAC 地址組合）

### 風險 2: 服務器端漏洞
**風險等級：中**
- 如果服務器端沒有正確限制試用次數
- 如果服務器端允許重複註冊

**防護措施：**
- 必須檢查服務器端代碼
- 確保每台設備只能註冊一次
- 確保試用次數正確扣除

## 📝 建議改進

### 1. 增強設備識別
可以考慮組合多個識別碼：
- Mac Hardware UUID（主要）
- MAC 地址（輔助）
- 系統序列號（輔助）

### 2. 服務器端日誌
記錄所有授權相關操作：
- 設備註冊時間
- 試用次數使用記錄
- 授權碼激活記錄

### 3. 時間限制
可以考慮添加試用期限制：
- 例如：3 次試用必須在 7 天內使用完
- 防止長期佔用試用名額

## ✅ 總結

**目前的授權系統：**
- ✅ 使用 Mac Hardware UUID 識別每台電腦
- ✅ 每台電腦的 UUID 都不同
- ✅ 服務器端控制試用次數
- ✅ 授權碼綁定到設備 ID

**確保安全性的關鍵：**
1. **服務器端必須正確實現**：每台設備只有 3 次試用
2. **服務器端必須防止重複註冊**：同一 deviceId 不能多次註冊
3. **服務器端必須正確扣除次數**：每次使用功能都要扣除

**結論：**
只要服務器端實現正確，目前的機制可以確保：
- ✅ 每台電腦只有 3 次免費試用
- ✅ 無法在不同電腦間共用免費試用
- ✅ 授權碼綁定到特定設備
