# 💳 購買和激活流程完整說明

## 📋 完整流程圖

```
用戶下載 App
    ↓
免費試用 3 次
    ↓
次數用完，提示購買
    ↓
前往網站購買
    ↓
選擇付款方式（綠界/藍新）
    ↓
完成付款
    ↓
收到授權碼（Email 或付款成功頁面）
    ↓
在 App 中輸入授權碼
    ↓
激活成功，無限制使用
```

---

## 🎯 詳細步驟說明

### 步驟 1：用戶下載並試用 App

1. **下載 App**
   - 從網站下載 macOS 或 Windows 版本
   - 開啟「開發者模式」（macOS 需要在系統設定中允許）

2. **首次使用**
   - App 會自動註冊設備（使用 Mac 硬體 UUID）
   - 自動獲得 **3 次免費試用**

3. **使用功能**
   - 每次使用「瞬移」或「散花」功能時，會消耗 1 次試用
   - App 會顯示剩餘次數（在「授權」頁面）

---

### 步驟 2：試用次數用完

當剩餘次數為 0 時：

1. **App 會顯示提示**
   - 「試用次數已用完，請購買授權」
   - 顯示購買按鈕或連結

2. **用戶點擊購買**
   - 跳轉到網站購買頁面
   - 或顯示網站 URL：`https://konggoo.tw`

---

### 步驟 3：在網站購買

1. **訪問網站**
   - URL：`https://konggoo.tw`
   - 點擊「立即購買」按鈕

2. **選擇付款方式**
   - **綠界科技**：支援信用卡、ATM、超商付款
   - **藍新金流**：支援信用卡、ATM

3. **填寫付款資訊**
   - 根據選擇的付款方式填寫相應資訊
   - 確認金額：NT$ 490

4. **完成付款**
   - 信用卡：立即完成
   - ATM：轉帳後等待確認（約 1-3 個工作天）
   - 超商：到超商繳費後等待確認（約 1-2 個工作天）

---

### 步驟 4：收到授權碼

付款完成後，用戶會通過以下方式收到授權碼：

#### 方式 1：付款成功頁面（立即）
- 付款完成後，會自動跳轉到成功頁面
- 頁面會顯示：
  - ✅ 付款成功
  - 📧 授權碼：`PKM-XXXXXXXX`
  - 📥 下載連結（如果需要重新下載 App）

#### 方式 2：Email 通知（如果實作）
- 系統會發送 Email 到用戶填寫的地址
- Email 內容包含：
  - 訂單編號
  - 授權碼
  - 使用說明

---

### 步驟 5：在 App 中輸入激活碼

1. **打開 App**
   - 啟動 Pikmin Architect App

2. **進入授權頁面**
   - 點擊右上角的「授權」標籤
   - 或從模式選擇中選擇「授權」

3. **查看當前狀態**
   - 會顯示「剩餘免費次數：0 次（請購買授權）」
   - 狀態會以紅色顯示

4. **輸入授權碼**
   - 在「輸入授權碼」欄位中貼上或輸入授權碼
   - 格式：`PKM-XXXXXXXX`（大寫字母和數字）

5. **激活授權碼**
   - 點擊「✅ 激活授權碼」按鈕
   - App 會連接到後端伺服器驗證授權碼

6. **激活成功**
   - 顯示「✅ 授權激活成功！」
   - 剩餘次數顯示會變為「已購買，無限制使用」（綠色）
   - 可以無限制使用所有功能

---

## 🔧 技術流程說明

### 後端處理流程

1. **付款完成後**
   ```javascript
   // server.js - /api/payment/return
   // 當綠界/藍新回調時：
   - 驗證付款結果
   - 生成授權碼：PKM-XXXXXXXX
   - 儲存到 licenses Map
   - 返回授權碼給用戶
   ```

2. **激活授權碼**
   ```javascript
   // auth_server.js - /api/license/activate
   // App 發送請求：
   POST /api/license/activate
   {
     "deviceId": "設備UUID",
     "licenseKey": "PKM-XXXXXXXX"
   }
   
   // 後端驗證：
   - 檢查授權碼是否存在
   - 檢查是否已使用
   - 綁定到設備
   - 更新設備狀態為 isPaid: true
   ```

### App 處理流程

1. **檢查授權狀態**
   ```swift
   // AuthManager.swift
   authManager.checkAuth { authorized in
     // 更新 UI 顯示
   }
   ```

2. **激活授權碼**
   ```swift
   // ContentView.swift
   authManager.activateLicense(key: licenseKey) { success, message in
     if success {
       // 顯示成功訊息
       // 更新剩餘次數顯示
     }
   }
   ```

---

## ❓ 常見問題

### Q1: 授權碼格式是什麼？
**A:** 格式為 `PKM-` 開頭，後面跟著 16 個大寫字母和數字，例如：`PKM-A1B2C3D4E5F6G7H8`

### Q2: 授權碼可以重複使用嗎？
**A:** 不可以。每個授權碼只能綁定到一個設備。如果需要在其他設備使用，需要購買新的授權。

### Q3: 付款後多久能收到授權碼？
**A:** 
- **信用卡付款**：立即收到
- **ATM 轉帳**：轉帳後 1-3 個工作天（需要人工確認）
- **超商付款**：繳費後 1-2 個工作天（需要人工確認）

### Q4: 激活失敗怎麼辦？
**A:** 請檢查：
1. 授權碼是否正確輸入（注意大小寫）
2. 網路連線是否正常
3. 授權碼是否已被使用
4. 如果問題持續，請聯繫客服

### Q5: 可以退款嗎？
**A:** 請聯繫客服處理退款事宜。

---

## 📞 客服聯繫

如有任何問題，請聯繫：
- **Email**：support@konggoo.tw
- **網站**：https://konggoo.tw

---

**最後更新**：2026-01-13
