# Pikmin Architect 完整購買、授權、金流指南

## 📋 目錄
1. [完整流程說明](#完整流程說明)
2. [金流整合步驟](#金流整合步驟)
3. [授權處理機制](#授權處理機制)
4. [跨平台支援方案](#跨平台支援方案)
5. [部署步驟](#部署步驟)

---

## 🔄 完整流程說明

### 用戶購買流程

```
1. 用戶訪問網站
   ↓
2. 點擊「立即購買」按鈕
   ↓
3. 選擇付款方式（信用卡/ATM/超商）
   ↓
4. 跳轉到金流平台付款
   ↓
5. 付款成功後，系統自動生成授權碼
   ↓
6. 授權碼顯示在成功頁面 + 發送 Email
   ↓
7. 用戶下載 App（macOS/Windows）
   ↓
8. 在 App 中輸入授權碼激活
   ↓
9. 開始使用完整版功能
```

### 系統架構

```
┌─────────────┐
│   網站前端   │ (index.html)
│  (購買頁面)  │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 網站服務器   │ (server.js)
│ - 創建訂單   │
│ - 生成授權碼 │
│ - 驗證授權碼 │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│  金流平台    │ (綠界/藍新)
│ - 處理付款   │
│ - 回調通知   │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 授權服務器   │ (auth_server.js)
│ - 設備註冊   │
│ - 授權驗證   │
│ - 試用管理   │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│  macOS App   │ (AuthManager.swift)
│ - 檢查授權   │
│ - 激活授權碼 │
└─────────────┘
```

---

## 💳 金流整合步驟

### 步驟 1：申請金流帳號

#### 選項 A：綠界科技（ECPay）⭐ 推薦
1. 前往 https://www.ecpay.com.tw/
2. 註冊帳號（個人或公司都可）
3. 填寫申請表單
4. 上傳身份證明文件
5. 等待審核（約 1-3 個工作天）
6. 審核通過後取得：
   - `MerchantID`（商店代號）
   - `HashKey`（金鑰）
   - `HashIV`（向量）

**費用**：
- 設定費：NT$ 0
- 手續費：信用卡 2.75%，ATM 1.5%
- 撥款：T+1（隔天撥款）

#### 選項 B：藍新金流（NewebPay）
1. 前往 https://www.newebpay.com/
2. 註冊帳號
3. 填寫申請表單
4. 等待審核（約 2-5 個工作天）
5. 取得 API 金鑰

**費用**：
- 設定費：NT$ 0
- 手續費：信用卡 2.5%，ATM 1.5%

### 步驟 2：設定環境變數

在 `website/` 目錄下創建 `.env` 檔案：

```env
# 綠界科技配置
ECPAY_MERCHANT_ID=your_merchant_id
ECPAY_HASH_KEY=your_hash_key
ECPAY_HASH_IV=your_hash_iv
ECPAY_RETURN_URL=https://your-domain.com/api/payment/return
ECPAY_ORDER_RESULT_URL=https://your-domain.com/payment/success

# 藍新金流配置（可選）
NEWEBPAY_MERCHANT_ID=your_merchant_id
NEWEBPAY_HASH_KEY=your_hash_key
NEWEBPAY_HASH_IV=your_hash_iv
NEWEBPAY_RETURN_URL=https://your-domain.com/api/payment/return
NEWEBPAY_NOTIFY_URL=https://your-domain.com/api/payment/notify

# 服務器配置
PORT=3001
ADMIN_KEY=your-secret-admin-key

# Email 配置（發送授權碼用）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

### 步驟 3：測試付款流程

#### 綠界科技測試環境
- 測試網址：https://payment-stage.ecpay.com.tw/
- 測試信用卡號：`4311-9522-2222-2222`
- 測試有效期限：任意未來日期
- 測試安全碼：任意 3 碼

#### 藍新金流測試環境
- 測試網址：https://ccore.newebpay.com/
- 測試信用卡號：`4000-2211-1111-1111`

### 步驟 4：啟動網站服務器

```bash
cd website
npm install
npm start
```

訪問 http://localhost:3001 測試購買流程

---

## 🔐 授權處理機制

### 授權碼生成流程

```javascript
// 付款成功後自動生成
const licenseKey = 'PKM-' + crypto.randomBytes(8).toString('hex').toUpperCase();
// 例如：PKM-A1B2C3D4E5F6G7H8
```

### 授權碼驗證流程

1. **用戶在 App 中輸入授權碼**
2. **App 發送請求到授權服務器**：
   ```swift
   POST /api/license/activate
   {
     "deviceId": "設備唯一ID",
     "licenseKey": "PKM-A1B2C3D4E5F6G7H8"
   }
   ```

3. **服務器驗證授權碼**：
   - 檢查授權碼是否存在
   - 檢查是否已被使用
   - 綁定到當前設備

4. **返回結果**：
   ```json
   {
     "success": true,
     "message": "授權激活成功"
   }
   ```

### 設備綁定機制

- **macOS**：使用硬體 UUID + Bundle ID
- **Windows**：使用硬體 UUID + 應用程式 ID
- 每個授權碼只能綁定一個設備（防止盜用）

### 試用機制

- 每個設備自動獲得 **3 次免費試用**
- 每次使用核心功能（瞬移、散花）消耗 1 次
- 試用次數用完後需要購買授權

---

## 💻 跨平台支援方案

### ✅ macOS 版本（已完成）

- **狀態**：✅ 已完成
- **技術**：Swift/SwiftUI
- **打包格式**：`.dmg`
- **系統要求**：macOS 10.15 以上
- **可以直接銷售**

### ⚠️ Windows 版本（需要開發）

#### 方案 1：使用 Electron（最推薦）⭐

**優點**：
- ✅ 可以重用現有的 `index.html` 介面
- ✅ 開發速度快（1-2 週）
- ✅ 跨平台支援好
- ✅ 維護成本低

**需要做的工作**：
1. 安裝 Electron：
   ```bash
   npm install electron --save-dev
   ```

2. 創建 `main.js`（Electron 主進程）：
   ```javascript
   const { app, BrowserWindow } = require('electron');
   
   function createWindow() {
     const win = new BrowserWindow({
       width: 1200,
       height: 800,
       webPreferences: {
         nodeIntegration: true,
         contextIsolation: false
       }
     });
     
     win.loadFile('index.html');
   }
   
   app.whenReady().then(createWindow);
   ```

3. 將 Swift 的 GPS 模擬功能改為 Node.js：
   - 使用 `pymobiledevice3` 的 Node.js 版本
   - 或使用 `node-ios-device` 套件

4. 打包為 `.exe`：
   ```bash
   npm install electron-builder --save-dev
   npm run build:win
   ```

**預估時間**：1-2 週

#### 方案 2：使用 .NET MAUI

**優點**：
- ✅ 原生性能好
- ✅ 微軟官方支援

**缺點**：
- ❌ 需要重寫大部分代碼
- ❌ 開發時間長（4-6 週）

#### 方案 3：使用 Flutter Desktop

**優點**：
- ✅ 跨平台支援好
- ✅ 一套代碼多平台

**缺點**：
- ❌ 需要重寫大部分代碼
- ❌ 學習曲線陡（4-6 週）

### 🎯 建議策略

1. **立即行動**：
   - ✅ macOS 版本已經完成，**可以立即開始銷售**
   - ⚠️ 同時開發 Windows 版本（使用 Electron）

2. **市場策略**：
   - 先推出 macOS 版本（立即可以開始銷售）
   - Windows 版本完成後，可以作為「新功能」宣傳
   - 兩個版本可以共用同一個授權碼系統

3. **開發優先順序**：
   ```
   第 1 週：完成 Electron 框架搭建
   第 2 週：移植 GPS 模擬功能
   第 3 週：測試和打包
   ```

---

## 🚀 部署步驟

### 方案 A：免費部署（推薦開始使用）

#### 使用 Vercel（完全免費）

1. **安裝 Vercel CLI**：
   ```bash
   npm i -g vercel
   ```

2. **登入 Vercel**：
   ```bash
   vercel login
   ```

3. **部署**：
   ```bash
   cd website
   vercel
   ```

4. **設定環境變數**：
   - 在 Vercel 控制台 → Settings → Environment Variables
   - 添加所有 `.env` 中的變數

5. **完成**：網站會自動部署到 `https://your-project.vercel.app`

#### 使用 Netlify（完全免費）

1. **安裝 Netlify CLI**：
   ```bash
   npm i -g netlify-cli
   ```

2. **登入**：
   ```bash
   netlify login
   ```

3. **部署**：
   ```bash
   cd website
   netlify deploy --prod
   ```

### 方案 B：付費部署（可選）

- **AWS EC2**：約 NT$ 300-1000/月
- **Google Cloud Platform**：按使用量計費
- **DigitalOcean**：約 NT$ 300-500/月

### 部署授權服務器

授權服務器（`auth_server.js`）需要單獨部署：

```bash
cd server
npm install
export ADMIN_KEY=your-secret-key
export PORT=3000
npm start
```

**建議部署位置**：
- Railway（免費方案）
- Render（免費方案）
- Heroku（付費）

---

## 📧 Email 發送授權碼（可選但建議）

### 使用 Nodemailer

1. **安裝套件**：
   ```bash
   cd website
   npm install nodemailer
   ```

2. **在 `server.js` 中添加**：
   ```javascript
   const nodemailer = require('nodemailer');
   
   const transporter = nodemailer.createTransport({
     host: process.env.SMTP_HOST,
     port: process.env.SMTP_PORT,
     secure: false,
     auth: {
       user: process.env.SMTP_USER,
       pass: process.env.SMTP_PASS
     }
   });
   
   async function sendLicenseEmail(email, licenseKey) {
     await transporter.sendMail({
       from: process.env.SMTP_USER,
       to: email,
       subject: 'Pikmin Architect 授權碼',
       html: `
         <h1>感謝您的購買！</h1>
         <p>您的授權碼：<strong>${licenseKey}</strong></p>
         <p>請在 App 中輸入此授權碼以激活完整版功能。</p>
       `
     });
   }
   ```

---

## 💰 成本估算

### 免費方案（推薦開始）

- **網站託管**：Vercel/Netlify 免費 ✅
- **授權服務器**：Railway/Render 免費 ✅
- **域名**：可選（約 NT$ 300-500/年）
- **金流手續費**：每筆交易 2.5-2.75%

**以 NT$ 490/筆為例**：
- 信用卡手續費：490 × 2.75% = **NT$ 13.48**
- 淨收入：490 - 13.48 = **NT$ 476.52**

### 付費方案（可選）

- **網站託管**：NT$ 300-1000/月
- **授權服務器**：NT$ 300-500/月
- **域名**：NT$ 300-500/年
- **金流手續費**：每筆交易 2.5-2.75%

---

## ✅ 檢查清單

### 立即可以做的
- [x] macOS 版本完成
- [x] 網站代碼完成
- [x] 金流整合代碼完成
- [x] 授權系統完成
- [ ] 申請金流帳號（綠界或藍新）
- [ ] 設定環境變數
- [ ] 部署網站到 Vercel
- [ ] 部署授權服務器
- [ ] 測試完整購買流程

### 同時進行
- [ ] 開發 Windows 版本（Electron）
- [ ] 整合 Email 發送功能
- [ ] 設定數據庫（替代內存儲存）

### 未來優化
- [ ] 添加用戶管理後台
- [ ] 添加統計分析
- [ ] 添加客服系統

---

## 🎯 下一步行動

1. **今天就可以做**：
   ```bash
   # 1. 申請綠界科技帳號
   # 2. 部署網站到 Vercel
   cd website
   npm install
   vercel
   
   # 3. 部署授權服務器到 Railway
   cd ../server
   # 上傳到 Railway
   
   # 4. 開始銷售 macOS 版本！
   ```

2. **本週完成**：
   - 測試完整購買流程
   - 確認授權碼發送正常
   - 準備宣傳材料

3. **下週開始**：
   - 開發 Windows 版本（Electron）
   - 完善 Email 功能

---

## 📞 需要幫助？

參考文件：
- `金流串接說明.md` - 詳細的金流申請步驟
- `授權系統說明.md` - 授權系統詳細說明
- `網站部署指南.md` - 部署詳細步驟
- `費用與跨平台說明.md` - 費用和平台支援說明
